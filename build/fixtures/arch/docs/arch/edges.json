{
  "edges": [
    {
      "id": "app-must-not-store",
      "from": "app",
      "to": "store",
      "kind": "imports",
      "rule": "must-not",
      "checker": "imports",
      "label": "no direct rows",
      "note": "The planted divergence. Two imports cross it and one of them is accepted.",
      "evidence": []
    },
    {
      "id": "core-must-vendored",
      "from": "core",
      "to": "vendored-lib",
      "kind": "imports",
      "rule": "must",
      "checker": "imports",
      "note": "The planted absence. Nothing imports it.",
      "evidence": []
    },
    {
      "id": "vendored-must-not-core",
      "from": "vendored-lib",
      "to": "core",
      "kind": "imports",
      "rule": "must-not",
      "checker": "imports",
      "note": "The conservative rule. One import out of this part did not resolve, so the answer is not green.",
      "evidence": []
    },
    {
      "id": "app-may-core",
      "from": "app",
      "to": "core",
      "kind": "imports",
      "rule": "may",
      "checker": "imports",
      "note": "A permission, which is what keeps the closed boundary quiet.",
      "evidence": []
    },
    {
      "id": "app-must-not-ajv",
      "from": "app",
      "to": "ajv",
      "kind": "imports",
      "rule": "must-not",
      "checker": "manifest",
      "note": "A dependency this project refuses, and the manifest agrees. It has been refused since 2019.",
      "evidence": []
    },
    {
      "id": "app-must-left-pad",
      "from": "app",
      "to": "left-pad",
      "kind": "imports",
      "rule": "must",
      "checker": "manifest",
      "note": "A dependency this project declares.",
      "evidence": []
    },
    {
      "id": "core-calls-store",
      "from": "core",
      "to": "store",
      "kind": "calls",
      "rule": "may",
      "checker": "evidence",
      "note": "A behavioural verb. It can never be better than partly checked.",
      "evidence": [
        {
          "path": "src/core/engine.ts",
          "lineStart": 3,
          "lineEnd": 3,
          "quote": "db.write("
        }
      ]
    },
    {
      "id": "dangling",
      "from": "app",
      "to": "nope",
      "kind": "imports",
      "rule": "must",
      "checker": "imports",
      "note": "Dropped whole, because no component file declares the far end.",
      "evidence": []
    },
    {
      "id": "app-may-core",
      "from": "app",
      "to": "store",
      "kind": "imports",
      "rule": "may",
      "checker": "imports",
      "note": "Ignored, because the first promise with this id is the one that counts.",
      "evidence": []
    },
    {
      "id": "app-must-not-vendored",
      "from": "app",
      "to": "vendored-lib",
      "kind": "imports",
      "rule": "must-not",
      "checker": "imports",
      "note": "The directory-grain pin, Phase 180 fix round and Phase 184. EVERY import across this line answers with a DIRECTORY and never a tracked file: a Swift target grain answer, and a C sharp PROJECT grain answer. Before the fix, fileOwners saw no such key, both crossings vanished from the ledger, and this must-not printed the false green. It is one row per arm that answers with a directory, because a fix nothing exercises for a new arm is a fix that arm does not have.",
      "evidence": []
    },
    {
      "id": "core-must-not-app",
      "from": "core",
      "to": "app",
      "kind": "imports",
      "rule": "must-not",
      "checker": "imports",
      "note": "The Go twin of the pin above. Go has answered with the imported package's DIRECTORY since Phase 63, and the same hole swallowed it. The only import across this line is main.go resolving to the directory src/app.",
      "evidence": []
    }
  ]
}
