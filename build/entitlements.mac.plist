<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the main Tortie app (research 27 section
  6.2). Keep this list minimal and justified: every entitlement is an
  attack-surface note in the notarization record.

  Deliberately NOT included: com.apple.security.cs.disable-library-validation.
  The native modules (node-pty, better-sqlite3), the unpacked ripgrep and the
  bundled specstory are all signed with the same team identity, so library
  validation is satisfied. If a future dependency forces that entitlement on,
  that is a paragraph in the phase brief, not a quiet addition here.
-->
<plist version="1.0">
  <dict>
    <!-- V8 JIT -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- V8 writable-executable pages on older codepaths -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Electron reads ELECTRON_* and DYLD_* style env in dev tooling paths -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
  </dict>
</plist>
