# Tortie — agent conventions

Electron + tmux shell for agentic coding. Product philosophy + name: docs/ZEN-OF-TORTIE.md. Architecture authority: docs/audits/2026-08-20-electron-typescript-architecture.md. Design authority: DESIGN.md + docs/DESIGN-SPEC.md. Work queue: docs/BACKLOG.md.

## The name (Phase 16.5, bundle id changed again in Phase 27)
The product is **Tortie** (`com.itavero.tortie` since Phase 27, `com.specstory.tortie` before that, `~/Library/Application Support/Tortie`). Tortie belongs to Ita Vero, LLC, the operator's company; the SpecStory INTEGRATION (the bundled specstory binary, src/main/specstory, the capture surfaces) keeps its name because it is a separate product Tortie talks to — never "finish off" that rename. The data directory follows `app.setName`, not the bundle id, so the Phase 27 id change moved no data. It was `gmux` until Phase 16.5, and much of the codebase's PROSE still says so — that is fine and deliberate. What is NOT prose, and must never be "finished off" by a later cleanup, is the set of identifiers live data is bound to: the tmux socket `-L gmux`, `resources/gmux-tmux.conf`, the `@gmux-*` session options, the `GMUX_SESSION_ID`/`GMUX_MANAGED` pane env, the inner `<userData>/gmux/` directory, the `window.gmux` bridge, the `gmux-asset:` scheme, `gmux.*` localStorage keys and `gmux-*` CSS classes. Renaming any of the first five strands sessions that are running right now. DEVELOPMENT.md has the full table and the reasons (README.md is product-facing). **User-visible copy is the only place the name may appear, and there it is always "Tortie".**

## Architecture invariants
- Sessions live in the PRIVATE tmux server (socket `-L gmux`, config resources/gmux-tmux.conf). The app is a disposable client. Never move durability-critical state into the app.
- Address live tmux sessions by immutable `$-id` (or `=`-exact name match), never bare names.
- The manifest (SQLite, main/manifest) is the source of truth for restore: argv + resume_argv always use ABSOLUTE binary paths. Agents are nonetheless LAUNCHED by bare name (Phase 12.7 F3): an absolute argv[0] made every durable gmux agent the one process on the machine that `pkill -f "$(command -v claude)"` matches. tmux's execvp finds the binary because the login-shell PATH is injected into the server env.
- Sessions are addressed by IDENTITY, never by name: `@gmux-id` (plus the `GMUX_SESSION_ID` pane-env stamp as the second source). A live session that carries neither is NOT OURS — never adopt it, never kill it.
- tmux SAFETY: only ever `tmux -L gmux`. Never touch the user's default tmux server, ~/.tmux.conf, or kill sessions you didn't create.

## Tortie never loads third party code (Phase 23) — the permanent refusals
These bind every future round the way the tmux safety rules above do. They are the outcome of docs/research/31-extensions.md, which examined bb, Zed and pi, wrote four competing architectures and had three adversaries attack each one. Eleven of the twelve reviews came back fatal. The single line that ended all of them is the first refusal.

The boundary, and it is the whole design:
> Configuration selects from choices the compiled world already contains, or names an executable the user has personally confirmed.

1. **No third-party JavaScript, TypeScript, WebAssembly or native code executes in any Tortie process.** Not main, not the renderers, not the preload, not a worker, not a `utilityProcess`.
2. **No `tortie.d.ts`, no SDK package, no contribution-point registry.** If a proposal begins "we will expose an interface so extensions can…", it is this refusal. bb froze 65 component prop types into a public contract and deleted it the next day.
3. **No marketplace, no store UI, no in-app browse-and-install, no update badge, no extension count on the activity rail.**
4. **No configuration mechanism may implement, replace, decorate or intercept** Explorer, SCM, search, the terminal, the tab spine, the manifest, the tmux layer, or Context's own data.
5. **No configuration mechanism may set a session's status.** Status semantics are in the UI rules below and they do not move.
6. **No third-party native code inside the signed bundle.** It would need `com.apple.security.cs.disable-library-validation` app-wide and permanently, against a configuration whose note reads "ZERO entitlements are needed".
7. **The main renderer's CSP is never relaxed.** Third-party HTML, if ever hosted, gets its own `session` partition and its own served CSP. `build/assert-preview-containment.mjs` asserts this at build time.
8. **Nothing may cause a process to start on a configuration change alone.** A human confirms the bytes, out of band of any agent turn, and the agreement is bound to a hash of the fields that decide what runs.

**Why refusal 8 is not theatre, stated so a later round does not remove it for convenience.** Every product cited as precedent for trusting configuration has a human as the only routine writer of that configuration, being Obsidian Restricted Mode, VS Code Workspace Trust, Zed, Raycast and pi. Tortie runs many agent processes at once under one user account, several deliberately launchable with their safeguards off, all with write access to the home directory. A configuration directory Tortie reads and an agent can write is an increase in privilege rather than a convenience. The gate has exactly one surface, being Settings then Agents, and removing that surface makes every configured agent unusable rather than making it convenient.

Two mechanical rules follow. The overlay type is hand written and narrow, and the internal registry type is never re-exported to it. An invalid row is dropped whole and surfaces as a visible error naming the field and the reason, never partially merged, never silently dropped, never a crash.

## Scope guardrail — gmux is not a VS Code reimplementation
gmux exists for what VS Code cannot do: durable named agent sessions (survive quit/crash/reboot with conversation resume), multi-project tabs in ONE window (VS Code refused this upstream — vscode#322745), and the agent layer (registry, per-agent icons/hotkeys/launch flags, agent-native status oracles, image drop, SpecStory capture). IDE furniture — git sidebar, decorated tree, editor tabs, markdown preview, minimap, search — is the price of admission, not the product.
Two rules follow, and they bind every future round:
1. **Justify parity work.** Before building any feature because "an IDE has it", answer in the proposal: *does this serve the agentic-coding workflow, or does it exist because IDEs have it?* If the latter, don't build it. (Passes: project-wide search — you must find things across repos agents are rewriting. Fails, and are explicitly deferred: structural/AST search, replace-in-files, LSP integration, debugging, task runners, extensions.)
2. **Assemble, never reimplement.** Prefer a maintained library or a vendored MIT extract over new code: Pierre for diffs/trees, Monaco for editing, ripgrep for search, VS Code's own git parsers and fuzzyScorer copied rather than reinvented, codicons + material-icon-theme for iconography. The code gmux owns should be glue and the differentiators above.
**Parity scope is capped after Phase 14 (search).** Everything after that goes to durability, the agent layer, correctness, and consolidation unless the user explicitly asks otherwise.

## The backlog is scanned from the bottom (operator's rule, 2026-08-21)

docs/BACKLOG.md ends with a section headed THE RUNNING LOG. **Append there, newest last, and never
reorder it.** Every phase that starts, every phase that lands and every new entry queued gets ONE
line, being the date, what happened, and the hash and version when it landed. The operator reads
this file by tailing it, so the end of the file must say where the queue is. It had drifted to a
research phase from four days earlier while six phases landed above it, which is what caused this
rule.

**The one exception he named.** An entry that was written down earlier but never queued MAY be
edited in place when it is finally queued, because its reasoning belongs beside the entries it
relates to. That is an edit to something that already exists. Anything NEW goes at the bottom.

**The log is an index, not a replacement.** Every phase still gets a FULL SECTION in the house
shape that the hundred phases above it use, and a one line entry is never enough to build from: the
`## Phase N` heading in the operator's words, the Subject, the First body line, the Semver, the Tier
with the reason for it, the Charter naming the entry and any research that binds it, the mechanism
written with real file paths read from the tree, the proof the phase must produce run rather than
read, and a **What is NOT in this phase** section, because the refusals are what stop a later round
widening the work. A phase entered as a single line has not been queued, it has been mentioned.

That new section is appended at the END of the file too, immediately above the running log, so the
file grows downward and the log stays last.

## Growth guardrails (enforced at every commit)
- One typed preload bridge derived from the shared contract in src/shared/ipc/ (domain files behind the index.ts facade, split in Phase 42) — never add a parallel wrapper "generation".
- Organize by domain, not by accretion — TypeScript best practices over line-count rules: one module = one responsibility with a small, deliberate export surface; split when a file accumulates unrelated domains or its internal sections need comments to navigate (per-domain store slices, per-domain ipc registrars, colocated component CSS), not because it crossed an arbitrary length.
- Grep for an existing helper before writing one. tmux binary/config resolution lives in exactly one module shared by supervisor and attach host.
- src/shared/* is append-only during parallel builds; integrators reconcile.
- After parallel work: scan for duplicated 10+ line blocks and extract.

## How every remaining phase gets built (the operating contract — do not lower this bar)
Each phase runs as ONE Workflow with the same shape that produced Phases 1-13: **spec -> parallel builders with disjoint file ownership -> integrator -> independent verifier(s) at the phase's tier -> a fix round if any verdict is needs_work -> commit per phase**. Non-negotiables:
- **Research before building** anything whose mechanism is not already measured, and write it to docs/research/ so the next agent inherits it instead of re-deriving. Several phases (13.5, 13.7, 14, 15) already have their research banked — use it.
- **Verifiers are independent of builders** and must produce EVIDENCE, not assurance: real app driving, measured numbers, byte-comparisons against ground truth, per-agent matrices where universality is claimed. A verifier that only reads code has not verified.
- **Fix rounds are part of the phase**, not follow-up. A phase is not done at needs_work.
- **Tier the verification** per the section below — do not default to maximum, do not skip Tier 3 where it is earned.
- **Commit per phase, conventional subject.** The subject is `type(scope): summary` (feat, fix, docs, refactor, test, chore, build, ci, perf, style; scope is one lowercase kebab word). The phase label is the FIRST BODY LINE, e.g. `Phase 24: self update`, and the build story stays in the body. No trailers of any kind.
- **Never leave the queue idle.** When a phase's workflow completes, immediately launch the next batch in the order recorded at the top of docs/BACKLOG.md. Do not wait to be asked. If a verdict blocks, fix it and continue.
- **Report to the user in their terms** when a phase lands: what they can now do that they could not before, and what is still not true.

## Machine discipline when running phases (rewritten 2026-08-23 with real numbers)

On 2026-08-22 the machine ran out of memory and crashed, `/private/tmp` was wiped and 163 files of
uncommitted builder work were lost. The rule written that day blamed concurrency and was wrong. The
numbers, measured on 2026-08-23: the machine has **48 GB**, one probe's Electron holds **241 MB**, and
the operator's entire running Tortie with all its sessions holds **1,524 MB across 18 processes**. Four
probes at once is under 1 GB. That was never the crash.

**The crash was a leak, not concurrency.** Of the 51 scripts under `build/` that started an Electron,
8 ended it in a `finally` block and the other 43 ended it only on the happy path. Any assertion that
threw left one running, and a retried or sequenced probe stacked them. Sixty stacked instances is
15 GB or more, and that is the machine. Phase 140 fixed it at the source. One helper,
`build/electron-run.mjs`, owns every launch and ends the tree it started in a `finally` block, and
`npm run gate:electron` is what keeps it there.

An earlier version of this section claimed Electron costs 451 MB per instance. That number was taken
from research 62, where it measures a MODEL SPAWN rather than Electron, and it should never have been
written here. Measure before writing a number into this file.

- **Probes may run concurrently, up to four at once.** Beyond four, queue them. This is a headroom
  rule rather than a safety rule, and the safety is the `finally` block.
- **Every probe that launches Electron kills it in a `finally` block**, and ends its own scratch tmux
  server there too, whatever happened. A probe that kills only on the happy path is a defect and the
  verifier names it. This is the rule that actually prevents the crash.
- **Every process a script starts, it ends in a `finally` block**, being a shell, a server, a
  sleeper or a load generator, whatever happened. A script that kills only on the happy path is a
  defect and the verifier names it. Phase 206 extended the Electron rule above to this in the same
  words, because on 2026-09-02 the Phase 200 verifier started six shell loops to load the machine
  for an attack, ran its probe, then tried to kill them; the kill did not reach them, the parent
  exited, they reparented to launchd and ran for two hours and three minutes at about 550 percent of
  his CPU until he noticed his fans. The conventions did not forbid it because they named Electron
  alone. `npm run gate:background` is what keeps it there, and it is described in the gates section
  below.
- **Count what is left after a probe run, once, at the end.** The command everybody reaches for,
  `ps aux | grep -c "[E]lectron"`, misses the largest process in the leak. Electron's main process
  renames itself to `Tortie`, and its command line is the single word `Tortie` with no arguments at
  all, so neither that grep nor a search for the profile path finds it. A deliberate leak measured on
  2026-08-23 held six processes and 521,520 KB, and the process both of those searches missed was
  179,984 KB of it. Count with
  `ps -Ao pid,ppid,rss,comm | grep -E "[E]lectron|Tortie$|chrome_crashpad" | grep -v defunct`.
  Every line except the bare `Tortie` one carries a `--user-data-dir` you can read under
  `ps -p <pid> -o command=`. The bare `Tortie` line is identified by its parent pid instead, which is
  the `node_modules/.bin/electron` shim your own run started. An entry marked `<defunct>` holds no
  memory and is not a leak. Do not count between every step. That bookkeeping cost 18 shell calls in
  one phase and prevented nothing.
- **Never send SIGKILL to the `node_modules/.bin/electron` shim.** That shim is a nine line Node
  forwarder. It passes SIGINT, SIGTERM and SIGUSR2 to the app it started, and nothing can forward
  SIGKILL. A SIGKILL to the shim kills the shim, the app reparents to launchd, and the app runs on.
  That was measured on 2026-08-23. The shim died and 482 MB of Tortie stayed up. Send SIGTERM first,
  wait for the pid to go, then SIGKILL the descendants by pid. Do not treat SIGTERM as enough on its
  own. On 2026-08-23 a leaked app was still up 16 seconds after one, and only the SIGKILL of the tree
  ended it. `build/electron-run.mjs` already does the whole sequence, so a probe should launch
  through `withElectron` rather than signal anything itself.
- **Two phase workflows may run at once** when the machine is quiet. Three or more only when none of
  them photographs.
- **Stop on swap AND pressure together, never on free pages.** macOS keeps free pages near zero on
  purpose, so that number is meaningless. Swap in use is not enough on its own either: on 2026-08-23
  swap read 5,039 MB while `memory_pressure` read 77 percent free, and every one of the top consumers
  was the operator's own work, being a Virtualization.framework VM at 2,662 MB, `tessl mcp` at
  1,995 MB, a node process at 1,724 MB and Chrome at about 3,300 MB. The alarm is
  `memory_pressure | grep percentage` under 20 percent free, and swap in use is the second signal that
  confirms it. Before stopping anything, read the top consumers by RSS and say whether they are yours.
  Never stop a phase for pressure another program caused.
- **A crash means RESTART FRESH, not resume.** `resumeFromRunId` replays an agent's TEXT and not the
  files it wrote, so a wiped or half-written worktree makes the cached reports a fiction. Reset the
  worktree to origin/main's tip before any resume.
- **NEVER `git reset --keep origin/main` in his checkout without checking for local commits first.**
  On 2026-08-23 that command discarded his own commit `a3bbd45`, "docs(audit): restore the
  architecture 36 plan", 517 lines he had just written. `--keep` protects uncommitted changes and does
  NOT protect a local commit that is not on the remote. The correct sync is
  `git -C /Users/gdc/gmux log --oneline origin/main..HEAD` first, and if it prints anything, stop and
  tell him rather than resetting. `git merge --ff-only origin/main` is the safe form, because it
  refuses instead of discarding. The file was recovered from the reflog at `13393f2`, and the reflog
  is the only reason it survived.
- **`/private/tmp` does not survive a reboot.** Rebuilding a worktree means `git worktree prune`,
  `git worktree add --detach`, then `cp -Rc node_modules` and `cp -Rc build/vendor` from the
  operator's checkout. Confirm `build/vendor/specstory/bin/specstory --version` before starting.
- **The exposure is structural and it is the price of committing once per phase.** The committer is
  the last agent, so a crash at any earlier point loses the whole phase. That trade is deliberate.

## Release notes (the operator set the style on 2026-08-23)

He rewrote every CHANGELOG entry by hand and said future release writing follows that style. The
rules are at the top of CHANGELOG.md and they bind every entry after. In short: one or two sentences
per item, what a person can now do or what no longer goes wrong, a limit a person will hit in one
clause in the same item, nothing a person will not hit, no numbers unless the number is the point, no
build story, no file names, no gate names. The lead paragraph says what the release is about in two
or three sentences and lists nothing. The long form with every measurement and every admission
belongs in the commit body, where it already is. Release pages carry the CHANGELOG entry verbatim,
and when CHANGELOG.md changes the release pages are synced to match.

## Verification (rewritten 2026-08-23 from what actually caught defects)

The old section said how MUCH to verify and never said what KIND. That is why a five item polish round
spent 64 minutes and ten app launches while a one line ruling caught the most important finding of the
day. Both axes are here now, and the second one is the governing rule.

### What actually caught something, measured over one day of phases

| What the verifier did | Where | What it caught |
| --- | --- | --- |
| Wrote its OWN detector by a different method | 123 | A second cycle detector, hand lexer and Kosaraju against the phase's AST and Tarjan. Disagreed by 3 edges, found the bug was ITS OWN, then agreed exactly |
| Attacked the ruling instead of confirming it | 128 | Refuted the stop question outright. The phase had judged the two COLDEST large files and never looked at the one with 30 commits in 6 days |
| Wrote its OWN hostile fixture | 137.1 | Twelve attack shapes the builder never tried, being svg onload, data: URIs, srcdoc, case mangled javascript: |
| Drove its own harness over REAL data | 137 | 35 real sessions across twelve providers, and one trap that leaked |
| Measured before and after on the PARENT commit | 139 | Minus 26px before and 0px after, and a rest photograph byte identical by md5 |
| Re-ran the builder's gates | 131 | npm test red, and an existing probe this phase broke and left broken |
| Read the code and reasoned about it | everywhere | Nothing, all day |

**The pattern is independence, not repetition.** Every finding above came from the verifier doing
something the builder did NOT do. Re-running the builder's own checks the builder's own way found red
gates and nothing else, which is worth its two minutes and no more.

### THE GOVERNING RULE

**The verifier must do at least one thing the builder did not.** Name it in the verdict. A verdict
whose evidence is only the builder's own checks re-run is not a verification, and a verifier that
reports approved without naming its independent step has not done the job.

The five independent methods, and pick the ones the risk earns:

| Method | Use it when | Cost |
| --- | --- | --- |
| **Re-derive independently** | A claim is computed, e.g. a count, a graph, a ratio, a parse | High, and it is the highest yield thing in this table |
| **Attack, do not confirm** | The phase concluded something, especially that it is finished or that something is impossible | Low, and it caught the biggest finding of the day |
| **Write a hostile fixture** | Anything that renders, parses or sanitizes bytes somebody else wrote | Medium |
| **Run over real data** | Anything claimed to work across agents, machines or providers | Medium, and it is the only proof of universality |
| **Measure the parent commit** | The operator reported it, or a number is claimed | Low, and it is the only honest proof a defect is fixed |

### The tier sets the BUDGET, and risk sets the tier

Answer these about the phase. Any yes takes the tier named.

- Can it lose or corrupt the person's work, being tmux, the manifest, restore or session lifecycle? **Tier 3.**
- Does it claim to work across every agent, machine or provider? **Tier 3**, and the evidence is a per row matrix over real data.
- Did the operator personally report it? **Tier 2 at least**, and the parent commit measurement is mandatory whatever the tier.
- Does it spawn a process, hold his credentials, or send his words anywhere? **Tier 3.**
- Is it invisible to a person? **Tier 2**, and the gates ARE the evidence, so the verifier re-derives rather than photographs.
- Is it a rendered surface with no new state? **Tier 2**, one app run.
- Is it copy, an icon, a token or a document? **Tier 1.**

| Tier | Budget |
| --- | --- |
| **Tier 1** | The gates. One photograph if the change is visual and cheap. No probe. |
| **Tier 2** | The gates, plus ONE app run that drives every claim in that one session, plus one independent method from the table above. |
| **Tier 3** | The gates, plus real data or a per row matrix, plus TWO independent methods, one of which is an attack, plus a fix round if any verdict is needs_work. |

### The four rules that stop the waste

1. **One app run per phase, not one per claim.** A probe launches the app once, drives every item, reads every rectangle and photograph it needs, and exits. Phase 137.2 used ten launches for five items and bought nothing a single session would have missed.
2. **Do not re-run a gate the same way twice.** Re-run the battery once to catch a red one. After that, spend the time on an independent method.
3. **Count Electrons once, at the end.** Not between every step. That bookkeeping cost 18 shell calls in one phase and prevented nothing.
4. **Mixed rounds verify per item at its own tier.** Do not promote a whole round to Tier 3 because one item earns it, and do not demote the item that earns it.

State the tier AND the independent methods in the phase brief, so the choice is deliberate and
reviewable before the work starts rather than after.

## Gates before any commit
`npm run typecheck && npm run build && npm run smoke:t1` minimum; integrators run the full battery (test, smoke, smoke:t3, package). Commit with a conventional subject `type(scope): summary`; the phase label goes on the first body line as `Phase N[.x]: summary`; no trailers.
**Touching the Context view's substrate table?** Add `npm run conformance:context` (about 1 s, spawns nothing, makes no request) for any commit under `src/main/context/agent-context.ts` or `src/renderer/context/groups.ts`. It prints the per-agent precedence matrix the panel actually draws from and fails when a row loses its model, its scope order or its reload answer. It is what keeps research 29 §2 executable rather than documented, and it is the gate that would have caught the panel stating Claude Code's ordering rule for every agent.
**Touching resume?** Add `npm run conformance:resume:capture` (~16 s, no turns, no tokens spent) to that list for any commit under `agents/registry.ts`, `manifest/harvest/**`, `manifest/agents.ts`, `sessions/codex-repair.ts`, `sessions/resume-argv.ts` or `restore/**` — it is the cheap gate that makes every registry resume claim executable, and it caught a one-word `availableAt` error that the whole battery above was blind to. The full `npm run conformance:resume` roundtrip (~3 min, real turns) runs once per phase and after any agent-CLI upgrade. `smoke:t3` covers a claude AND a non-claude restore shape; neither is a substitute for the other.
**Touching the agent table?** Add `npm run conformance:agents` (~2 s, spawns nothing, opens no manifest, launches no Electron) for any commit under `agents/registry.ts`, `manifest/agents.ts`, `main/config/**` or `renderer/state/agents.ts`. It proves four things the unit tests do not: the create path composes an absolute argv, a resume argv built from the parsed manifest row alone equals the registry's byte for byte, the renderer's seed list agrees with the registry, and the confirm hash moves for every execution bearing field and for none of the presentation ones.
**Touching the install map?** Add `npm run conformance:installs` (about 1 s, spawns no agent, makes no request) for any commit under `src/main/agents/registry.ts`. It asserts the six shape rules from research 47 §10, and it is what keeps the map's promise, that nothing in it can run, checkable instead of asserted.
**Touching machines?** Add `npm run conformance:machines` (about 1 s, starts no ssh, opens no file under the person's home, launches no Electron) for any commit under `src/main/machines/**`. It proves the confirm hash moves for `host`, `user`, `port` and `remoteTmuxPath` and for neither presentation field, that a machine and a configured agent with the same bare id cannot share one agreement, that an invalid row is dropped whole with the field named, that `BatchMode=no` exists at exactly one call site, and that the connection test argv names Tortie's own host key file FIRST and the person's second. The last one replaced a weaker rule that passed while the product added three lines to the operator's `~/.ssh/known_hosts`, measured at 932 bytes before a probe run and 1229 bytes after.

**Touching the remote feed's own maps?** Add `npm run conformance:remoteclose` (about 0.6 s, starts no ssh, opens no manifest, launches no Electron, spawns nothing at all) for any commit under `src/main/machines/remote-sessions.ts` or `src/main/sessions/core.ts`'s remove path. It drives the real feed over 25 closes per arm and proves the property Phase 187 was reported for: a machine's `rows` and `gone` maps are DISJOINT, a session drawn once is drawn once, and ONE Remove clears the row whatever holds it. Phase 187's second fix round added five arms for the shape the first one left open, being a list ISSUED BEFORE a close answering AFTER it: the pass now refuses to reinstate an id a Remove cleared from a snapshot that predates the Remove. It goes red one arm at a time under ablation, 25 returns out of 25 with the removal instant dropped, 25 returns out of 25 with the `||` restored, and 25 rows drawn twice with the pass's `gone` delete dropped, so it is a guard that can fail rather than one that recites. `npm run probe:p187` is the same drive over a loopback machine of its own and takes minutes, which is why it is not in the commit battery; its RACE arm is the real data proof of the same thing, 3 of 3 back at the parent and 0 of 5 at HEAD.

**Touching the overview reader, the keep map or the overview store?** Add `npm run conformance:overview` (about 3 s, spawns no agent, opens no Electron, starts no tmux server, reads nothing under the person's home) for any commit under `src/main/overview/**`. It prints the per provider slot matrix, the keep ratio and the trap count over the committed fixture corpus, proves the seven defects research 63 section 19 named stay fixed, proves every trap in section 16 stays caught, proves redaction on both sides against every secret shape found, and proves the store survives a kill mid write. A vendor change shows up as a ratio that moves before it shows up as an empty page.

**Touching a harvest descriptor, the sub agent predicate or the codex repair?** Add `npm run conformance:derived` (about 2.6 s, spawns no agent, opens no keychain, starts no tmux server, launches no Electron, makes no request and reads NOTHING under the person's home, because it writes every fixture itself into a scratch directory it removes in a `finally`) for any commit under `src/main/manifest/harvest/derived.ts`, `codex-state.ts`, `stores.ts`, `watch.ts`, `remote.ts` or `src/main/sessions/codex-repair.ts`. **A RESUME ID NAMES A SESSION, NEVER ONE OF ITS SUB AGENTS**, and until Phase 215 nothing in this product said so for codex. He rebooted on 2026-09-05 and codex refused one of his sessions with `cannot resume an unloaded multi-agent v2 sub-agent through its parent`: the id in his row was a sub agent thread, because the codex descriptor keys on `cwd-newest` and its confirm read ONE field of line 1, being `payload.cwd`, which a sub agent inherits from its parent VERBATIM, and a sub agent is always NEWER than the thread that spawned it. 165 of the 185 rollouts in his September shards are sub agents, so it was nine in ten. Tortie already refused this for muse, whose task-streams live in their own DIRECTORY, and it missed codex because codex writes them into the same date shard under the same filename shape, so only the CONTENTS tell them apart. **The fix is a REQUIRED FIELD rather than a patch to codex**: every entry in `DESCRIPTORS` declares `derivedStream`, it has no default so a new agent does not compile without one, `none` is a CLAIM carrying the evidence for it rather than a silence, and the pipeline asks it once for every agent BEFORE any key is applied, so a future `cwd-newest` agent inherits the protection instead of re-earning it and muse's path rule became one implementation of the shared question. deepseek, pi and omp were each MEASURED read only rather than assumed and answer `none` with how the store was searched; pi's `measured` names the trap, being that the `parentId` on its later lines is the record chain inside one transcript and not a session parent, so an implementer reaching for it would refuse every pi session. The gate's eight rules are the declaration with its checker proved on four planted descriptors, the predicate over 15 record shapes, the parent read from the nested place as well as the top level because 115 of his 521 derived records name it only there, the walk over 13 chains with `resolved` null on every refusal and the bound pinned at the measured 8, FOUR call sites scanned over the real source including the REMOTE rung whose codex arm re-implements the confirm by hand, the repair over nine row shapes proving no row is emptied on any path and that it is idempotent by digest, fourteen ablations of the pure module that must each turn a pinned answer red naming the clause, and the REMOTE ARM'S REACH. **THE FIX ROUND ADDED THAT LAST ONE AND CORRECTED THE PHASE'S OWN CLAIM ABOUT IT.** The phase called the remote arm the finding a fix confined to `stores.ts` would have missed and said it stops a connected machine taking sub agents exactly as this Mac did, and over his own store it stops nothing, because a codex `session_meta` line is far bigger than the 8,192 bytes `REMOTE_HARVEST_HEAD_BYTES` hands that rung: the shortest line 1 of his 523 derived rollouts is 13,798 bytes, the longest 22,298, the largest line 1 anywhere 34,526, and only 171 of his 25,453 session records fit inside 8,192 at all, so the shipping arm answers `unknown` 523 of 523 at the shipped budget and `mismatch` 200 of 200 when handed the whole line. It was taking nothing before the fix either, because `decideRemoteHarvest` accepts a verdict of exactly `match`, so an `unknown` produces no claim at the parent commit or at HEAD. Rule 8 therefore neither assumes reach nor pins the budget: it READS the budget out of the live half, drives the shipping arm at sizes computed on both sides of it, fails if a sub agent is ever called a match at any size, and fails if an `unknown` or a `mismatch` ever wins. **The same round made two counted numbers counted.** Rule 5 printed `4 call sites found` as a hardcoded string, and printed it in the same breath as naming two call sites it had just failed to find, which is the shape the known-hosts paragraph above exists to forbid; it now prints how many answered, reading `2 of 4` against a tree carrying the parent's `watch.ts`. And three shipped comments said the six agents answer `none` while rule 1 printed five in the same output, so the count is now counted from `DESCRIPTORS` and rule 1 fails the build when a prose site states a different one, the scanner proved on four planted texts including the sentence that shipped. **Codex states the answer in a database and Tortie was inferring it**: `${CODEX_HOME:-~/.codex}/state_5.sqlite` carries `thread_source` and an explicit `thread_spawn_edges` parent map, the registry has named that file since Phase 12 as a fast path recorded for its SPEED, and nobody noticed it also answers is this a session and who is its parent. It is a second voice and never a replacement, because the column post-dates 25,038 of his 25,973 rows and the rollout catches two records the store says nothing about. **It is opened READ ONLY and NEVER when a `-wal` exists without its `-shm`**, because a read-only open in that state CREATES the `-shm`, measured on this tree with better-sqlite3 itself, and his `~/.codex` is writable; `immutable=1` is refused too, because it creates nothing and is STALE, and a stale answer here is a wrong resume id. The boot repair walks `parent_thread_id` to a thread it has proved exists and is not itself derived, and **every failure path leaves the row exactly as it is and reports it**, because a wrong id a person can see beats a row that quietly emptied. Driven over a COPY of his live manifest, his own file never opened for writing: 25 rows byte identical, 19 with no id, 4 repaired one hop, 0 left alone for any other reason, pass 2 moved nothing and the digests match. The same drive against a codex home holding only the rollouts moves the same four rows to the same four ids, which is the two voices agreeing over real data. **The live proof that the repaired id opens is UNMEASURED and is his**: it needs `codex resume` under his own login, and no agent may spend his turn.

**Touching the file watcher?** Add `npm run conformance:watcher` (about 1 s, opens no FSEvents stream, starts no tmux server, launches no Electron, runs no git) for any commit under `src/main/watcher/`. `FSEventStreamSetExclusionPaths` accepts at most EIGHT paths, and above that it does not truncate: it returns false and applies ZERO exclusions, `@parcel/watcher` never checks the return value, the stream still starts, and nothing is logged. So a ninth exclusion added by any future round would silently disable all eight including `.git`, and the only symptom would be a machine that got slower. The dotgit subscription already passes five, so it is four away. The gate scans every `watcher.subscribe` call site under `src/main`, runs the exclusion planner over nine root counts to prove it never exceeds the budget and never loses a root, and proves its own scanner on six fixtures. The measurement behind the number 8 is `build/fsevents-cap.c`, re-runnable as `npm run conformance:watcher:cap` (about 25 s, macOS only), which is deliberately NOT in the commit battery.

**Touching a script under `build/` that starts an Electron?** Add `npm run gate:electron` (about 0.1 s, spawns nothing, opens no profile, launches no Electron) for any commit under `build/`. It runs inside `npm run build` too, so nothing that builds can skip it. It asserts that no file under `build/` except `build/electron-run.mjs` hands an Electron program to a spawn, that the POPULATION of scripts reaching that helper is not shrinking, that the helper's kill is inside a `finally` block read by matching braces rather than by searching for the word, and it proves the scanner on three fixtures it writes itself and the population floor on two counts. **That second rule was a hand list until Phase 219 and the hand list is the defect it exists to refuse.** It named 56 files while 87 reached the helper, so 30 real scripts had drifted off it going back to Phase 140, and the gate printed a green sentence saying "56 reach build/electron-run.mjs" that was the list's own length rather than a measurement. The set is derived now and the assertion is `HELPER_USER_FLOOR`, one integer measured at 86 on 2026-09-06 with `node build/assert-electron-teardown.mjs --list` and raised to 87 the same day when the fix round added `probe-p219-geometry.mjs`. **A commit that adds a probe raises the floor in that same commit**, because adding one cannot turn this gate red, so a floor left behind is one that would let the new probe be deleted again in silence. Adding a probe can never turn the gate red; deleting one, renaming one or taking one off the helper does, and a deliberate deletion lowers the floor in the same commit and names the file in the commit body. Deriving the set WITHOUT the floor would be a tautology, since a set computed by `usesHelper` and checked with `usesHelper` cannot disagree with itself, which is the vacuity the rule was written for. It is what stops the 2026-08-22 crash coming back. 43 of the 51 scripts that started an Electron ended it only on the happy path, so any assertion that threw before the kill line left about 480 MB running. The gate earned its place before it shipped. Phase 138 landed a probe that started an Electron of its own while Phase 140 was being written, and this gate is what found it.

**Touching the credential store?** Add `npm run conformance:credentials` (about 30 s since Phase 220 took its ablations to 60, launches no Electron, starts no tmux server, spawns no agent, makes no request, OPENS NO KEYCHAIN and reads nothing under the person's home) for any commit under `src/main/credentials/`. It runs the SHIPPING domain under node over an injected keychain written from the measurement, and it is the gate on the one thing in this product that writes a person's credential. It proves the capture, the promotion of the account a `/login` replaced into a login named from its address, a round trip matrix over every ordered pair of three accounts with all three intact at every hop, the interrupted write stopped after each of its three steps leaving the old credential or the new one and never neither, the rollback of a staged copy that does not read back equal, the person's own location refused as a write target by both providers, the refusal to write a store a session is running under, a store caught mid change not being captured, six attack shapes, and no token byte in any answer, any refusal, either file or ANY command line, over an arm that really runs the keychain path so the command lines exist to be checked. **Four of its arms are the fix rounds' and they are the ones a later change is most likely to undo.** Two OVERLAPPING observes must leave one login, a surviving record row and two answers that agree, because a visit to the Agents page issues more than one list at once and the unserialised read modify write under that destroyed the rescued account's row permanently while thirteen ablations passed. A store that names NO account on either side must still keep what it replaced, because a login signed into a moment ago has no address until it takes a turn and that is exactly when a person types `/login` again; the cost is bounded in the same arm, ten refreshes leaving one login rather than nine. And nothing may be left holding a credential in the staged place beside a store, which real kills left in two arms of three. **The fourth is the one arm that uses REAL files and real links, and it is the committer's.** Every write in this domain stages at a name nobody has opened yet, and `writeFile` follows a link, so an entry planted at `<login dir>/auth.json.tortie-pending` took the whole write, read back through itself so the check passed, and put the kept credential into a file standing in for the person's own `~/.codex/auth.json` while the write answered ok; the commit then renamed the link onto the store. `src/main/logins/dirs.ts` already guards the login DIRECTORY against that exact shape because the Phase 202 verifier found one in the running app, and this phase put a file write inside that guarded directory without extending the guard to the file. `src/main/credentials/nofollow.ts` is the whole fix, being unlink then an exclusive create so a re-planted link fails the write rather than being followed, and an `lstat` in front of the rename. A bag of strings has no links in it, which is how the defect survived thirteen ablations. Twenty ablations, one clause each, must all go red. `npm run probe:p204` (about 35 s, one Electron on a scratch profile and the p204 socket, spawns no agent, spends no token, opens no keychain, drives add, switch, switch back and a simulated store change over FIXTURE stores only) is the app run and is not in the commit battery.

**PHASE 211 LIFTED TWO PHASE 204 REFUSALS AT THE OPERATOR'S WORD, AND THE SAME GATE CARRIES THEM.** On 2026-09-03 he said a switch did not reach the running session and a `/login` was not seen until something asked. Both were Phase 204 rules: `activateLogin` refused a store a session was running under, and `stores.ts` refused the person's own default location for every write. So `activateLogin` now WRITES while a session runs, and it writes the vendor's own default location when a session of the provider runs under the default login, through `defaultStoreTarget`, which the gate proves is reached from exactly one call site and which `storeTarget` still refuses as its first act. Every claude write is held under Claude Code's own three locks, spoken from `src/main/credentials/locks.ts` as the installed bundle speaks them: `<config-home>/.oauth_refresh.lock`, then the legacy `<realpath of config-home>.lock`, both stale at 60 s and touched every 5 s, then `<config-dir>/.storage-write`, stale at 15 s; a live lock is never stolen, a stale one is, a held lock past the nine second wait is a refusal naming the lock, and codex holds none because it has none. **The committer's round put the READ inside the lock, and that order is the rule.** The fix round promoted the outgoing account before the write, which was right, but read the store, promoted and moved the record BEFORE taking the locks, so a refresh the vendor saved while the lift waited for them was written over after the release with no copy anywhere, which is the exact race the locks were ported to close; `liftStore` in `keep.ts` now takes the locks, THEN reads, keeps what it read, promotes, writes and releases, and the gate's ablation is those two lines swapped. The same round closed two more losses: a held copy of the same account is brought up to the newer bytes and the record remembers the one digest it moved past so the next observe does not mirror the stale store straight back (the round trip work, alice, work now hands the session the refreshed token), and the default promotion asks the SLOT rather than the store, so a default store that reads as empty, as garbage or as half a credential at the lift still keeps the account. Rules 6 to 6k carry it, and 50 ablations one clause each must all go red. A `/login` is seen at once by `src/main/credentials/watch.ts`, `fs.watch` on the exact files a sign in changes inside the credentials domain and NOT a subscription through `src/main/watcher`, so the FSEvents exclusion budget is untouched and `conformance:watcher` proves it; the keychain half is polled by attributes only as the thirty second backstop. And every change a person makes pushes `logins:changed` from the one function the registrar answers through, so a choose in one window reaches every other; `conformance:logins` rule 11 reads that push from the function's own braces over four fixtures. `npm run probe:p211` (about two minutes, one Electron on a scratch profile and the p211 socket, spawns no agent, spends no token, opens no keychain) is the app run and is not in the commit battery. The live pickup on a real vendor process is UNMEASURED and rests on the bundle's thirty second constant, for the reason docs/research/79 gives.

**PHASE 208 SCOPED THE VAULT TO ITS PROFILE, AND THE SAME GATE CARRIES IT.** Until then `vaultServiceFor` answered `Tortie-credentials-<slot>` with no profile in the name while the vendor half had carried a directory digest since Phase 203, so every Tortie process on the machine, being his running app and every scratch profile probe under `build/`, addressed the SAME keychain items, and the Phase 206 fix round measured his `Tortie-credentials-claude.default` moving twice inside probe runs on scratch profiles. The name is now `Tortie-credentials-<slot>-<eight hex of sha256 of the logins root>`, his root gives `23b8c4e8`, and `keychainVault(runner, scope)` requires both arguments so the root cannot be dropped again. THE MIGRATION ANSWER IS REWRITE AND DELETE, for Phase 206 item 1's reason: `src/main/credentials/migrate.ts` is the ONLY file that composes the unscoped name, it refuses as its first line unless `isOwnProfile` proved the launch is not a harness launch and its userData is exactly `<appData>/<appName>`, so a scratch profile cannot compose the old name at all, and the old item is deleted only after the scoped one reads back equal. Every production caller goes through `readyKeepDeps`, which runs the move once per process in front of the first observe. A HARNESS LAUNCH THAT INSTALLED NO KNOB NOW GETS THE FILE VAULT with a refusing `security` seam and `home` inside the profile, because scoping alone would have let every fixture-less scratch launch CREATE a scoped copy of his credential in his login keychain, and the boot observe would have done it on every launch; `GMUX_HARNESS_KEYCHAIN` is the one new knob, refused unless the keychain file itself sits inside `GMUX_HARNESS_DIR`, and under it the real keychain path runs over a scratch file. THE STORES ARE ALSO OBSERVED ONCE AT BOOT now, a second after the core is up and never awaited, so Phase 206's two sweeps run on every launch rather than on the first list; `[gmux-logins] logins.boot` prints what it cost, measured at 232 ms and 11 `security` runs on a scratch profile and derived at about 35 runs on his own first launch and 17 after. Rule 17 of `conformance:credentials` proves a scratch root and his root compose different names, that no name from any root equals the unscoped one, the migration both ways, and the two scans, with five ablations that each go red. `npm run probe:p208` (about 40 s, one Electron on a scratch profile and the p208 socket, spawns no agent, spends no token) is the app run and is not in the commit battery: it makes a keychain of its own with `security create-keychain` under the harness directory, never in the search list and deleted in a `finally`, plants a credential in the scratch default store, reads it back out of the scoped slot in that keychain under the profile's digest, proves an unscoped plant beside it was left alone, drives the migration matrix over the real `security` on the same file, and inventories his own keychain by attributes before and after with NO `-g` and NO `-w` against it. **The parent measurement is refused on purpose**, because red at the parent is a write to his keychain, and the gate's digest ablation stands in for it. **PHASE 219 CLOSED THE THREE THINGS THE PHASE 208 VERIFIER RECORDED AND NOBODY FIXED, and the same gate carries them.** The profile proof is a VERDICT rather than a boolean, `ownProfileVerdict`, because `resolve` follows no link and so a home or an Application Support reached through one had one side of the comparison spelled through the link and the other spelled around it: both MIXED shapes read false and the migration was refused for ever, with `refused: true` in one log line as the only trace, indistinguishable from not being on macOS and from a probe's own deliberate refusal. `realpathSync` is now asked SECOND, only when the strings already disagree, so it can turn a refusal into a pass and never the other way round, and a scratch profile moved by `--user-data-dir` still really is a different directory and still reads `elsewhere` from either spelling. `keychainDelete` answers a BOOLEAN, and `MigrateResult` gained `reason` and `failed`, because it answered `void` and every caller counted the asking rather than the doing: a runner answering `security`'s exit 44 to all six deletes of a migration left `{moved: 0, deleted: 2, kept: 0}` in the result with both old items still on the machine. And the probe LOADS `migrate.ts` only if it is there, answering `scope: {absent: true}` when it is not, because every ablation this gate has is a text edit and none of them can express the shape that actually broke it, being a file that is not there at all: run at the parent of Phase 208's own code the gate died on a raw `ERR_MODULE_NOT_FOUND` wrapped as "the probe did not run" and named no rule, and a gate that dies is not a gate that fails. Rule 17 now names itself, and a staged copy of the domain with `migrate.ts` removed is an arm of the gate rather than a text ablation. 53 ablations, was 50.

**PHASE 211 LIFTED TWO MORE AT HIS WORD, AND ITS FIX ROUND IS WHY THE GATE HAS ELEVEN MORE ARMS.** A switch now WRITES the store a session is running under, and the vendor's own default location when a session of the provider runs on the default login, held under Claude Code's own THREE locks in the vendor's order, being `<config-home>/.oauth_refresh.lock`, the legacy `<realpath of config-home>.lock` and `<config-home>/.storage-write`, read from the 2.1.259 bundle with the offsets in docs/research/79. The verifier found the default lift losing the person's own account in the ordinary sequence, because an observe promotes only on a change and at that moment nothing had changed: the lift wrote the chosen bytes into the keychain item while `~/.claude.json` still named the outgoing account, and the next observe read "same account" and overwrote the only copy. `liftDefaultStore` in `src/main/credentials/keep.ts` now promotes the default slot's account BEFORE a byte moves and moves the record on to the chosen account AFTER, under the root lock. Run `npm run conformance:credentials` for any commit under `src/main/credentials/` as before; it now also proves the lift with a stale identity file (7f), a held lock answered as a refusal naming the lock rather than thrown (7g), an unmakeable lock refused at once and the null branch sleeping (7c), three locks in order and every one released (7c), the legacy lock named from the real path over a real link, a login made after the watch started being watched once told (7h), the keychain backstop fingerprinting `mdat` rather than the account attribute the vendor never changes (7i), and the read seam refusing a link (15b), with eleven more ablations that each go red, 46 in all. `npm run probe:p211` (about 90 s after the build, ONE Electron on a scratch profile and the gmux-p211 socket, two stub vendors that record their pane's environment, a stub login shell, no token spent, his keychain inventoried by attributes with no `-g` and no `-w`) is the app run and is not in the commit battery: it drives the lift on a live default session, reads the sentence and `Restart now` off the toast, presses it and reads the replacement's row and its pane's `CODEX_HOME` from inside, and times the watcher's redraw of an outside sign in with no hover and no visit. The number on the face is the bundle's thirty second keychain cache and not a stopwatch: no vendor process was driven, because timing one needs a scratch keychain first in the search list and that is `security list-keychains -s` against his own; his acceptance step in the entry is the reading that can confirm it.

**Phase 204 lifted one Phase 202 refusal, and it was the operator's decision.** Phase 202 said Tortie never writes a credential byte. On 2026-09-02 he weighed that against what it costs, being that `/login` inside a session overwrites the credential in that store and the account that was there is gone, and chose the orca behaviour. So Tortie now keeps a copy of every account it sees in a store, promotes the account a store moved away from into a login of its own, and writes a kept account back into a store when a person chooses that login. **Every other Phase 202 refusal stands**: no agent ever signs anybody in, the vendor's own command is the only thing that authenticates, there is no refresh and no rotation, no running session is switched, no provider beyond claude and codex, and the person's own default store is READ ONLY FOREVER, refused by name in `src/main/credentials/stores.ts` and proved by the gate.

**PHASE 220 GAVE THE DOMAIN A SHUTDOWN OWNER AND MADE A SWITCH SAY WHAT HAPPENED, AND THE SAME GATE CARRIES BOTH.** Run `npm run conformance:credentials` for any commit under `src/main/credentials/` as before, and now for any commit under `src/main/capabilities.ts`'s ordered disposer, `src/main/logins/ipc.ts`'s choose handler or `src/main/proc/guarded.ts`, whose `runGuarded` gained an optional `stdin` so this domain's one write reaches `security -i` with no payload on any argv. **RULE 18 IS THE SESSION EVIDENCE, AND IT IS THREE ANSWERS RATHER THAN TWO.** `activateLogin` asked which sessions are running as `.catch(() => [])`, and since Phase 211 that answer decides whether the person's own default location is written, so an answer that could not be had was read as "nothing is running": measured over the shipping code at `b5cc017`, a rejected query with a default session live and a working query with no sessions at all both answered `ok=true "one.example is signed in again."`, and in the first of those the running agent still held the account the person had just switched away from with nothing said. The ask now happens ABOVE every write an activation makes. An UNAVAILABLE answer refuses, leaves the selected login exactly as it was, writes no store and says that is what happened; a KNOWN answer, empty or not, behaves exactly as Phase 211 left it, running session and default store included; and the rule holds all three arms against each other so the two that must not move cannot move in silence. **NOTHING IS ROLLED BACK when an activation throws**, because a vendor refresh may have landed in the same window and writing over it is the loss the locks were ported to stop: a throw with nothing confirmed answers a refusal naming the store that MAY have changed, since `swap.ts` guarantees old-or-new and never says which, and a throw after a confirmed write answers the Phase 211 shaped partial that says which half did not happen. **RULE 19 IS THE ONE SHUTDOWN OWNER**, `src/main/credentials/lifecycle.ts`, and that file opens with the safe interruption points because THEY ARE WRITTEN BEFORE ANY CANCELLATION IS WIRED, which is the order the brief asked for and is the rule: before the vendor's locks are taken, where `locks.ts` refuses in a sentence naming the lock and is asked BEFORE the mkdir, so a lock the vendor holds is never taken and so never stolen; inside `swap.ts` at any of its four steps, where the store already holds the old credential or the new one and never neither; and NEVER between the keep and the write, because `liftStore` promotes the account a store is about to lose before a byte moves and refuses outright when it could not. A write already past its lock is not cancelled by a flag and runs to completion. Admission closes on the disposer's FIRST LINE before any await, and the join happens before `shutdownGmuxCore()` closes the manifest. What is owned is the accepted observations including the one a change REPLACES, the boot observe, the vault migration, the lock waits, the activation, the remove, the `security` children, which are ended by the handle that spawned them and never by name, and THE LATE WATCH START, which is the shape the audit never named: a quit landing inside the boot chain's one second wait ran `stopLoginsWatch()` against `watch === null` and the chain then installed `fs.watch` handles and an interval AFTER the ordered disposer had finished with this domain. An idle quit resolves in the same tick, starts no process and logs nothing, which is why `smoke:t1` is unmoved. The gate's ablations are 60 and every one must go red. **THE FIX ROUND ADDED THE POSITION AND CHANGED WHAT A REMOVE DOES IN THE QUIT WINDOW.** The verifier ablated the phase's own checks and found three that could not fail. The join's PLACE, above `shutdownGmuxCore()`, was a sentence in a comment and nothing else: moving the join below the core shutdown left this gate printing OK with all six of its disposer fixtures behaving, so `disposerRegistersTheOwner` now asks the position too and there are NINE fixtures, one of which is the moved join. It is pinned twice, because the gate runs for a commit under `src/main/credentials/` and the position lives in `src/main/capabilities.ts`: `src/main/__tests__/quit-dispose-order.test.ts` asks the same two questions on every `npm test`, over the body with its comments stripped, since the sentence explaining the join contains the word `await` twice. The other two were tests rather than code: two arms of `shutdown_refuses_late_login_work` sat inside `observeAll`'s five second hold so they read nothing whatever admission said, and the `trackCredentialWork` around `activateLogin` was held up by nothing at all because `src/main/logins/ipc.ts` is outside the directory this gate ablates. **AND A REMOVE THAT ARRIVES DURING THE QUIT NOW REFUSES WHOLE**, rather than skipping `forgetLogin` and forgetting the row anyway, which was Phase 206's order inverted for the sake of milliseconds.

**Touching the redline?** Add `npm run conformance:redline` (about 3 s, launches no Electron, starts no tmux server, spawns no agent, makes no request, reads nothing under the person's home) for any commit under `src/renderer/editor/redline.ts`, `redline-copy.ts`, `RedlineRow.tsx`, `redline.css` or the clipboard knob in `src/main/harness/shot.ts`. It runs the SHIPPING module under node and judges what it printed, so sixteen rulings stay executable rather than documented: the prose allowlist, the round trip on both sides over emoji with a zero width joiner, combining marks, a right to left run and Japanese, an independent re-derivation by a hand written LCS, the anchors re-derived against a line level LCS and against a patch whose line numbers differ from its array indexes by 39, all three caps and the worst case they allow, the three refusals scanned over the real source, the spacing only ruling, the accounting that every change block is drawn or counted, the run order kept as jsdiff returns it rather than tidied into pairs, and the harness putting the person's own pasteboard back inside a `finally`. Every scanner is proved on fixtures the gate writes itself, so a scan that cannot fail is never mistaken for a scan that passed. `npm run probe:p191` (about 4 min, one Electron on a scratch profile and a scratch tmux socket, spawns no agent, spends no token, writes and restores the system clipboard) is the app run and is not in the commit battery. **Phase 225 added `src/renderer/editor/baseline.ts` to the files rule 9 scans, and its fix round added the two pins the phase shipped without**: the redline's left side is the tab's shadow baseline and not the HEAD version, which is one argument at the compose site in `RedlineDocument.tsx`, and with that argument put back to the parent's every gate in the battery stayed green. `src/renderer/editor/__tests__/p225-redline-projection.test.tsx` renders the shipping view with `renderToStaticMarkup` and takes the projection property off the markup, being that the non-INS children concatenate to the baseline and the non-DEL children to the working text, over the one shape that tells the two sides apart, an untracked file after its first watcher tick; it goes red with the argument put back. `npm run probe:p225` (about three minutes after the build, ONE Electron on a scratch profile, a scratch HOME and the gmux-p225 socket, spawns no agent, spends no token, opens no keychain, writes files from outside through a synchronous `/bin/sh`) is the app run and is not in the commit battery: it takes the same property off the live DOM inside the real EditorPanel tree at fifteen of its nineteen readings, 81 checks in all, and reads the generation at every step, so the four things that must never move the baseline and the one that must are numbers rather than sentences. `--self-test` proves its grader on seven fixtures and launches nothing. **PHASE 227 GAVE THE REDLINE ITS FIRST CONTROL AND ITS FIRST WRITE, keyboard only, and narrowed rule 9 rather than deleting it.** The redline draws one `span.ed-redline-change` per change, being research 83 B.2's unit of adjacent non-`same` runs, `tabindex` -1 with the baseline offset, the deleted text, the inserted text and the drawn generation on it, and the projection property is unchanged because it is read at the leaves; `p227-redline-changes.test.tsx` pins the eight wrappers of the B.1 paragraph attribute for attribute and both projections. The press is pure in `src/renderer/editor/rewind.ts`, being the ported `.p222/fix/mix.ts` mix, the identity resolution by `(offset, del, ins)` and the decision, and `p227-rewind.test.ts` pins it over the eight change fixture and all 256 subsets. The ONE call site that writes is `redline-write.ts`, whose `applyRewind` asks the baseline generation guard before the re-read before the guarded write; `redline-journal.ts` is the per-tab in-memory undo stack, the chords and the Edit-menu rows are the keymap's `redline.*`, and `redline-sentences.ts` is one sentence per refusal word. **Rule 9 was narrowed, never deleted**: the redline may name exactly one write channel at exactly one call site, and the declared function holding it asks the guard first and the re-read second, read by matching braces, with the scanner proved on seven planted redline sets. **Six arms were added, one per loss research 83 measured**, each a pure computation over two strings through the shipping `rewind.ts` via `build/redline-rewind-probe.mts`: the stale draw (B.4d), the truncated read in both shapes (E.7a), the moved baseline generation (B.8a), the path outside every root (E.5), the person's own insertion (A8a, which rewinds AND undoes byte for byte), and the encoding round trip (E.7b); every arm goes red under an ablation of its clause, driven over a copy of the four value modules of the chain, removed in a `finally`. Add `conformance:redline` for any commit under `src/renderer/editor/rewind.ts`, `redline-write.ts`, `redline-journal.ts`, `redline-sentences.ts`, `redline-press.ts`, `RedlineDocument.tsx`, `redline-commands.ts` or the keymap's `redline.*` entries as well. **THE FIX ROUND MOVED THE PRESS OUT OF THE VIEW AND ADDED A SEVENTH ARM, because the journal recorded the wrong change.** `press()` read `focusedChange(host)` a second time AFTER `await applyRewind(...)` to build the journal entry, and a press is two IPC round trips: the verifier moved the focus with ⌥↓ inside that window through the real chord and E0 was rewound on disk with the journal holding E1, so undo said "no longer in the file" and E0's rewind was recoverable from nothing Tortie keeps, the exact A8a loss the journal exists for; moved onto the pure insertion E6, undo WROTE "exactly " a second time. `src/renderer/editor/redline-press.ts` is the press now, pure of React and the DOM, the focus reader, the one call site and the refusal injected as functions, the identity read ONCE before any await and the journal moved with that same object, and an undo pops the entry it wrote back by reference and never the top. The seventh arm of rule 8 drives the SHIPPING press under node with a call site that moves the focus inside its await and its ablation is the first shape put back; `p227-redline-press.test.ts` pins the same two shapes under `npm test`. **Rule 9's file set is DERIVED now**, every file under `src/renderer/editor` named `redline*`, `Redline*`, `rewind.ts` or `baseline.ts`, fourteen with a floor of fourteen, because the verifier planted a guarded write in `redline-commands.ts` and the hand list did not name it; a deliberate deletion lowers the floor in the same commit. The write is counted as a MENTION and not a call, because `const w = b.fs.writeGuarded; w.call(b.fs, ...)` counted as nothing; nine planted sets. **THE SAME-SIZE WRITE INSIDE THE CHANNEL'S `lstat`-to-`rename` WINDOW IS REAL AND IS A STATED LIMIT OF PHASE 226'S CHANNEL**, read by the verifier at 15 of 25 `wrote` answers under a racer rewriting every ~50 µs and re-derived by the fix round at 1 of 5 over 32,921 rewrites in eight seconds, with 7,319 `stale` and 79 `raced` catching everything outside it; no editor or agent writes at that rate, the close needs `renamex_np`, which Node does not expose, and `redline-write.ts`'s header carries the numbers. `probe:p167`'s redline surface now rewrites the file under itself `P167_REWRITES` times an open, in-process, which is the charter line Phase 225 left to this phase. `npm run probe:p227` (about three minutes after the build, ONE Electron on a scratch profile, a scratch HOME and the gmux-p227 socket, spawns no agent, spends no token, opens no keychain, writes files from outside through a synchronous `/bin/sh` and rewinds only files inside the scratch project) is the app run and is not in the commit battery: it draws an agent edit, jumps to a change with ⌥↓, presses rewind with ⌥⌫ and reads the file FROM DISK with the phrase back and every other edit standing, undoes with ⌥⇧⌫, has a shell write the pressed phrase between the draw and the press and reads the refusal sentence with the shell's bytes surviving, rewinds and undoes the person's own saved paragraph, and reads the resting face to prove nothing is drawn on it, 17 checks with the operator's `-L gmux` sessions read 13 before and 13 after. **PHASE 238 GAVE THE REDLINE ITS ACCEPT, which is the same function pointed at the other destination, and added RULE 18 with five arms.** Research 83 B.3 and B.5: rewinding change `e` writes THE FILE as `mix(runs, E \ {e})` and leaves the baseline, while accepting `e` writes THE BASELINE as `mix(runs, {e})` and leaves the file with its md5 unchanged, so **a rewind writes his prose file and an accept writes only Tortie's own shadow copy**. `acceptChange`, `acceptAll` and `planAccept` are pure in `src/renderer/editor/rewind.ts`; `src/renderer/editor/redline-accept.ts` is the press and is SYNCHRONOUS, because there is no read and no write to await, so there is no window for the focus to move in and no journal to record. The baseline advances through the one place that already owns it, `nextBaseline`, with an `accept` origin and `acceptedAt`, and **THE GENERATION MOVES ON EVERY ACCEPT**, which is the point: every drawn rewind identity is an offset INTO the baseline, and this is the first gesture in the product that moves it often. Rule 18 drives research 83 B.8a's own document through the SHIPPING press with a real accept moving the generation between the draw and the press, and it is not a tautology because the same arm reads that WITHOUT the guard the identity resolves to exactly one change and it is the SECOND lorry; 18a is all 256 subsets accepted one at a time with the picture re-derived between, 18d is a digest of a REAL FILE before and after with a rewind in the same breath so the reader is shown able to see a write, and 18e is accept-all leaving one run and zero changes without ever asking what is focused. Five ablations, all red, and its fixture directories are `.p238-accept-*` at the repository ROOT rather than inside `src/`. **RULE 9 WAS NARROWED AND NOT WEAKENED**: `acceptChange` was in `FORBIDDEN_WRITE` as a PROXY for "an accept writes a file", which was true only while there was no accept, so banning the name now bans the safe half and bans nothing dangerous; the real property is asked structurally, an accept reaching a file must name `writeFile`, an `fs:` channel or the bridge and all three still fail everywhere but the one call site, and behaviourally by 18d. **ACCEPT ALL HAS NO CHORD**, which is the entry's second option: asking once needs a native dialog, which needs an invoke channel, which rule 9 forbids the redline from reaching, so the keystroke was removed rather than a question added; it is `.ed-redline-bar`, the redline's own header, drawn only while there is something to accept, and an Edit-menu row with no sublabel that `view-menu.test.ts` asserts absent. Accept joins Rewind on the chip at ⌥↩. Add the gate for any commit under `src/renderer/editor/redline-accept.ts` and the accept half of `baseline.ts` as well; rule 9's derived file floor is 20. **`probe:p167`'s redline surface accepts under itself now**, `P167_ACCEPTS` per open, and it found the defect the phase shipped without: an accept removes the wrapper the keyboard was on, Chromium sends focus to `document.body`, and the scroller's React key handler never sees the next chord, so 6 of 12 accepts never fired; the host is focused before React re-renders and 12 of 12 land. **THE REWIND HAS THE SAME SHAPE and is NOT fixed here**, because its recompose arrives through the watcher rather than in the press's own tick; it is written down in `RedlineDocument.tsx` beside the fix.

**Touching the guarded write?** Add `npm run conformance:redline-write` (about eighteen seconds, launches no Electron, starts no tmux server, spawns no agent, makes no request, reads nothing under the person's home, and writes every fixture itself into a scratch directory it removes in a `finally`) for any commit under `src/main/fs/guarded-write.ts`, the `fs:writeGuarded` registration in `src/main/fs/ipc.ts`, or `FsGuardedWriteInput`, `FsGuardedWriteResult` and `READ_CAP_BYTES` in `src/shared/fs-ops.ts`. It is the gate on the one channel that will rewrite the person's file when Phase 227 wires the redline's rewind to it, and until then the channel is reachable from nothing, which rule 5 asserts and Phase 227 deletes. It drives the SHIPPING channel under node over 28 readings, being the four refusals and the three protections the charter names plus the fix round's arms, and re-runs the probe over fifteen ablated copies of the module, one clause each, failing unless every copy moves a reading and naming which. **THE FIX ROUND'S THREE FINDINGS ARE THE PARAGRAPH, because each is a clause a later round could take out again.** A NAMED PIPE at the path hung main: `open(2)` of a FIFO with no writer blocks until one arrives, on whatever thread called it, and one IPC call with a path the renderer supplies froze the whole app for 5,002 ms until it was killed. The read carries `O_NONBLOCK` now, the same open returns in 0 ms and the pipe is refused `io` by the regular-file check; the arm drives it in a child of its own PROCESS GROUP with an eight second deadline, ended whole in a `finally`, because a signal to tsx's outer node alone leaves the inner one blocked in the open and reparented to launchd, which is how the verifier found it. THE WINDOW AFTER THE HASH WAS NOT MICROSECONDS: the module's own header and research 86 both said so, and the measurement is 23.6 ms for a file at the cap, the time to write the payload, inside which a real appender lost lines on 4 of 4 `wrote` answers. The `lstat` already in front of the rename now compares inode, size, mtime and ctime with the `fstat` the read took and answers `raced` on any difference, so an append, a whole swap, a same-size rewrite in place and a bare chmod each leave the agent's bytes where they are; the staged copy is asked the same question against the descriptor that wrote it, so a REGULAR file swapped in at the staged name, which no link check sees, is refused too. The comparison is ablated whole and by its ctime clause alone, because ctime is the one field that decides on its own. And a file whose owner write bit is clear was REPLACED with `wrote` and its mode kept, where a plain save answers EACCES, because `rename` asks the directory's permission and never the file's; it answers `readOnly` now, a word of its own in the shared type, read from the owner bit and not the caller's rights so it is the same answer under every uid, with the root-owned file the directory still lets a rename replace as the stated limit. Rule 3 scans the entry point's own braces for `O_NONBLOCK` beside `O_NOFOLLOW` and `O_EXCL`, proved on seven planted texts of which six must fail. The remaining stated limits are in the module header: a hard-linked target is replaced at this name only, a directory planted at the staged name is refused and left, and a change of the same size inside one timestamp tick is invisible, which on APFS is a nanosecond.

**Touching the reading partition, the facts or the sentence?** Add `npm run conformance:reading` (about 1.2 s, one plain node, launches no Electron, starts no tmux server, spawns no agent, makes no request, reads nothing under the person's home) for any commit under `src/main/arch/skeleton.ts`'s rule P, `src/main/arch/reading.ts`, `src/main/arch/sentence.ts`, `src/main/arch/tree-facts.ts` or the reading fields `src/main/arch/map.ts` composes. It runs the SHIPPING partition and composer over three committed fixtures under `build/fixtures/reading/`, a gmux shaped tree, a Cargo workspace and a multi client tree with Swift and Kotlin targets, plus two trees it builds itself to reach P3's floor and P4's cap, and pins the box set, rule R and every rule S sentence byte for byte with the ten hover facts behind them, then runs the same probe over seventeen ablated copies of the directory, one clause each, and fails unless every copy turns a pin red, naming the clause. Research 77 measured the reading at nine useful sentences in ten over gmux, rookery and ripgrep, and that fraction decays the day a clause moves silently, which is what the ablations are for. `P201_PROJECT=<a copy of a repository> npm run probe:p201` (about two minutes, one Electron on a scratch profile and a scratch tmux socket, spawns no agent, spends no token, refuses the checkout it runs from and proves the repository was only read by comparing `git status` before and after) is the app run and is not in the commit battery: it flips the Architecture switch in its own profile, opens the pane with its chord, waits for the cold scan and the tree read to land, reads the header icons, the repository line, the model slot, every row's sentence and hover facts and the contract's place off the DOM, compares them to the approved mock when the repository is the gmux copy, opens the map and checks it draws the sidebar's boxes, and drills from a row.

**Touching the file walk?** Add `npm run conformance:filehistory` (about 3 s, launches no Electron, starts no tmux server, spawns no agent, makes no request, reads nothing under the person's home) for any commit under `src/main/git/service.ts`'s `walk`, `src/main/git/parse.ts`'s `readNameStatusChunk` or `src/main/git/graph-parse.ts`. It builds a ten commit fixture holding a copy, a rename to a name with `*` and `[` in it, an in place rewrite, a delete and a merge, runs the SHIPPING walk over it and pins every row's status, path and old path, and then runs the same probe over three ablated copies of the shipping modules, the newline strip taken out of the chunk reader, `--follow` taken out of the argv and `--topo-order` put back under follow, each of which must go red. The third one is the finding this phase was built on: on git 2.50.1 `--topo-order` changes what `--follow` returns, 607 rows against 609 over `builtin/log.c` in git's own repository and 8 against 9 on the fixture, the missing rows being ordinary edits on a side branch merged after the rename, so a followed walk drops the flag. `P198_PROJECT=<a copy of a Tortie checkout> npm run probe:p198` (about two minutes, one Electron on a scratch profile and a scratch tmux socket, spawns no agent, spends no token, refuses the checkout it runs from; the copy is named in the environment because the harness script reads one command and nothing after it, so a `-- --project` on the npm line never arrives) is the app run and is not in the commit battery: it right clicks the fixture's row in the Explorer, has main answer the row's own menu with History under the shot mode knob `GMUX_SHOT_POPUP_PICK`, reads the 31 rows off the DOM against git's own `--follow` walk sha for sha, clicks the rename boundary and reads a two sided diff back from the editor store, then walks the journey to the last tab closed.

**Touching the history search?** Add `npm run conformance:historysearch` (about 2 s, launches no Electron, starts no tmux server, spawns no agent, makes no request, reads nothing under the person's home) for any commit under `src/renderer/scm/history-search.ts`, `src/main/git/search-args.ts`, `src/main/git/exec.ts` or the search branch of `src/main/git/service.ts`'s `graphLog` and `walk`. It runs the SHIPPING parser, the SHIPPING argv composer and the SHIPPING service under node over thirty attack shapes, being a value that is only a dash, `--all`, a quote, an author with `[` and with `\|`, a path that globs, a path that escapes, a commit that is not a commit, an operator alone and a line break inside every operator at once, and proves that every value is ONE argv element in its attached form, that `--` sits before the pathspec and `--end-of-options` before the name, that no element holds a line break, that an operator alone composes nothing, and, over a six commit fixture it builds itself, that the rows are what the values mean literally and that two walks fired at once on one queue end with the first rejected. It scans `service.ts` for a `--grep`, `--author` or `-S` of its own and for the line that drops `--topo-order` under a search, proves the scanner on two plants, and runs the composer four times ablated, `--grep=` detached, `-S` detached, `--` taken out and `--end-of-options` taken out, each of which must go red. `P199_PROJECT=<a copy of a Tortie checkout> npm run probe:p199` (about two minutes, one Electron on a scratch profile and a scratch tmux socket, spawns no agent, spends no token, refuses the checkout it runs from) is the app run and is not in the commit battery: it types into the field one character at a time and reads each keystroke's walk time off the store, narrows by author, path and commit, compares every row set to git's own answer for the same query composed with its own argv, expands a row and opens a file from it, presses Load 50 more with the query kept, runs the changes button and reads its printed time, types a burst faster than the debounce and proves only the last query drew, and presses Escape to get the plain walk and its gutter back.

**Touching logins?** Add `npm run conformance:logins` (about 4 s, launches no Electron, starts no tmux server, spawns no agent, makes no request, reads nothing under the person's home) for any commit under `src/main/logins/`, `src/shared/logins.ts` or the login layer in `src/main/sessions/launch-plan.ts`. A login is a directory the vendor's own CLI signed into, and the person's DEFAULT login is their own forever, so the gate's first rule is a scan rather than a run: no file in the logins domain may name `.claude`, `.codex`, a home directory or `node:os` at all, because what it cannot name it cannot write to and cannot delete. It also proves there are exactly TWO deletion calls in the domain, being `removeLogin` and `removeStrayLoginDir`, and that the function holding each asks `isOwnedLoginDir` first, read by matching braces rather than by searching the file for the word. **That number said ONE until Phase 219's fix round and the gate had pinned two for phases**, which is this file's own item 10 class: a sentence that reads as a measurement and is not one. Then it runs the SHIPPING store under node over a scratch root: thirteen ownership shapes including the other provider's tree and a parent traversal, a hostile store file whose every row is dropped whole with its reason pinned, a planted directory that must survive a remove aimed at it, no token byte and no directory in the manifest row or either argv over a fixture credential holding a sentinel, and the confirm hash byte identical with a login chosen while the same hash is shown to MOVE when `launch.env` grows, so that half is a measurement rather than a constant. Every runtime rule is re-run over an ablated copy of the domain, one copy per rule, and must go red; two of those ablations carry more than one edit on purpose, because the domain guards every path twice and removing one guard alone changes nothing a person could see. `npm run probe:p202` (about 25 s of driving after the build, TWO Electrons one after the other and never at once, on one scratch profile and the p202 tmux socket, spawns no vendor binary at all, spends no token, runs no sign in, and reads the person's credentials only to hash them before and after) is the app run and is not in the commit battery. It drives 45 readings over a fixture second login per provider: the pane environment read from INSIDE the pane, the login name on the row, the plan the meter reached, the card read off the DOM, restore by name after the server that held the sessions is gone, the fallback with its sentence when the login is removed, and the attacks, being a switch mid launch, a deleted directory, a hand edited store file naming a directory Tortie does not own, and posts from the wrong login. `claude` and `codex` in that run are two stub scripts the probe writes, which record the environment their pane really got; a RESTORED pane is a shell, so that reading is taken by asking the pane itself the way the operator does by hand, and it is checked against what tmux was told at the spawn. The meter is answered by a FILE through `GMUX_USAGE_FIXTURE` with the keychain refused, installed only for a harness launch on a harness profile, and the probe waits for main to say so before it arms a meter. `node build/probe-p202-logins.mjs --self-test` proves its graders on thirteen fixtures and launches nothing.

**PHASE 203 ADDED THREE RULES TO THAT SAME GATE AND ONE PROBE BESIDE IT.** Run `npm run conformance:logins` for any commit under `src/main/usage/login-accounts.ts` and `src/shared/login-copy.ts` as well. Rule 8 is PRESENCE: a login whose credential exists only in the KEYCHAIN reads as present, which on macOS is the only half there is, because the vendor writes no `.credentials.json` for a second claude login and the old list asked for one, so every added claude login said `Not signed in yet` for ever; a login asks for the SCOPED service name and nothing else, so it can never read the person's own default credential; the scoped name is re-derived by a sha256 the gate computes itself; and a folder that is GONE is never asked about, because a Remove deletes the folder and leaves the scoped keychain item behind. Rule 9 is the ACCOUNT: both providers name an address, codex out of the `email` claim inside `tokens.id_token`, and a login that has taken no turn, a file that is not JSON and a field holding markup are all "not known" rather than a crash or a drawn string. Rule 10 is scanned over the real source: `-w` is the flag that makes `security` print the SECRET instead of the item, so it appears nowhere in the reader, and the reader writes no log line of any kind. Every ablation goes red one clause at a time, twelve of them when Phase 203 wrote this and sixteen since Phase 219, and 14 of 14 scanner fixtures behave. **The identity read spawns nothing at all**, which is stronger than the phase promised: a login that has been USED carries its own address in its own `.claude.json`, and `claude auth status` scoped to a login with no `oauthAccount` answers `email: null`, so the vendor command tells us nothing the file does not, at 140 ms and one process against 0.05 ms and none. **The reader is in the USAGE domain and not the logins one** because rule 1 above forbids the logins domain from naming a vendor location, and that refusal is what keeps the person's own sign in out of reach of the one deletion; `src/main/logins/ipc.ts` reaches it as an injected function. `npm run probe:p203` (about 16 s of driving after the build, TWO Electrons one after the other and never at once, on one scratch profile and the p203 tmux socket, creates no session, spawns no agent, makes no request, spends no token, and opens NO KEYCHAIN because the usage fixture refuses it for the list as well as for the meter) is the app run and is not in the commit battery. It writes SIX fixture logins, one per shape a row can take, being the default and an added login each signed in with an address, signed in without one, and not signed in at all, and reads every row back off the list, off the native menu the shipped composer would be handed, off the hover card opened with a real pointer event, and off the real Settings window's own DOM. It plants the decoy `~/.claude/.claude.json` on purpose, because a reader that composed the default ACCOUNT file the way the default CREDENTIAL file is composed would find that one and answer "account not known" for the default login. `node build/probe-p203-account.mjs --self-test` proves its graders on thirteen fixtures and launches nothing.

**PHASE 219 ADDED TWO RULES TO THAT SAME GATE AND FOUR ABLATIONS BESIDE THEM**, and both are findings a verifier recorded and nobody fixed until the fifth nits round went back for them. Rule 3c is the CREATE path. The only guard in front of the two `mkdir`s was `isOwnedLoginDir`, which opens nothing and is a pure string compare, so a provider root that is a SYMBOLIC LINK is spelled perfectly inside the logins root, passes it, and Add login makes a folder in a directory Tortie does not own; the row is dropped at every later read, which is what kept it a nit. `loginAncestorIsLink` in `src/main/logins/dirs.ts` is the ancestor test lifted out of `loginDirOnDisk` so a create path can ask it before the folder is there, since a component that is NOT THERE is not a link, and BOTH HALVES OF THE STRAY SWEEP ask it: `strayLoginIds`, because `readdirSync` follows a linked root and every sixteen hex name it reads under one would be swept, AND `removeStrayLoginDir` itself, because `finishStraysOnce` in `src/main/credentials/keep.ts` unions that answer with the ids of Tortie's own recorded vault slots that no row names, and that second source never passes through `strayLoginIds` at all; driven over a real symlink, an id aimed straight at the delete took a planted victim directory whole. The guard is where the delete is, which is that function's own stated rule; the ENTRY is still not asked, because a stray that is itself a link must stay finishable. The fixture is a REAL symlink on a real disk in probe section 2c, with the victim directory counted before and after, because a refusal that leaves a folder behind is not a refusal, and an ORDINARY root is driven in the same breath so a guard that refuses everything cannot read as a pass. Rule 3d is the record: a row whose `id` is a JSON NUMBER made `namedLoginIds` answer the set the rest of the file justified, so a login the record NAMES read as a stray and the sweep deleted its folder and the credential inside it. Any element that is not a plain object carrying a string `id` now answers null for the WHOLE file, which is that function's own stated rule, because for it a set missing an id is a delete order rather than a smaller answer; the cost of null is a sweep that does not run. Sixteen ablations, was twelve, and each goes red one clause at a time. The sixteenth is the delete site's own, and it is there because the fifteenth was pinned by nothing but the sentence that removed it: taken out by hand instead of by the gate's ablation text, every live rule stayed green, since no arm had ever driven a sweep under a link. Every guard in this domain is now pinned by BEHAVIOUR, with the same planted directory swept normally under a root with no link in it so a guard that refuses everything cannot read as a pass.

**AND ITS FIX ROUND FOUND THE SECOND DOOR, WHICH IS THE PARAGRAPH ABOVE'S OWN SENTENCE LEFT UNCHECKED.** Rule 3c reasoned that `strayLoginIds` must ask the ancestor test because `removeStrayLoginDir`'s "own ownership rule is the string one and passes" — and then guarded the caller and not the delete. Two things followed. First, the new guard was pinned by NOTHING but this gate's own ablation text: taken out of `strayLoginIds` by hand, every live rule stayed green and the only failure was "the ablation found nothing to edit", because probe section 2c asks the PREDICATE and section 2d drives the sweep over PLAIN roots, so no arm ever drove a sweep under a link. Second, the door it did not guard is REACHABLE: `finishStraysOnce` in `src/main/credentials/keep.ts` unions `strayLoginIds`'s answer with the ids of Tortie's own recorded vault slots that no row names, and that second source never passes through `strayLoginIds` at all. Driven at the parent of the fix over a real symlink, an id aimed straight at `removeStrayLoginDir` under a linked provider root deleted the planted victim directory whole: one entry holding a credential before, none after. So the ancestor test is asked in `removeStrayLoginDir` too, where the delete is, which is the domain's own standing rule rather than a new one, and the ENTRY is still not asked because a stray that is itself a link must stay finishable. Probe section 2e drives BOTH doors and counts the victim on both sides, with the same planted directory swept normally under a root with no link in it so a guard that refuses everything cannot read as a pass. **Sixteen ablations now**, and each of the two goes red on its own reading: the caller's on "a linked provider root was walked for strays", the delete's on the aimed refusal and the victim count. `p206-strays.test.ts` carries the same pair, so `npm test` catches it as well as the gate.

**PHASE 220 ADDED RULE 12 TO THAT SAME GATE, AND IT IS THE OTHER END OF `conformance:credentials`'s RULE 18.** Run `npm run conformance:logins` for any commit under `src/main/logins/ipc.ts` as before; rule 12 is that the `logins:choose` catch must LEAVE rather than fall through. At the parent it fell through to `chooseLogin` two lines below it, so with `activateLogin` made to reject, the real registered handler answered `ok: true`, `logins.json` recorded the new name, the person was told nothing, and every new session under that login launched with whatever bytes were in the store. The rule reads that catch from the handler's own braces the way rule 11 reads the push, over 5 fixtures of which 4 must fail, and the ablation restoring the fall-through goes red. Ablations are 16. **The braces both rules are read by come from one helper rather than two**: `functionBodyOf` lives in `build/scan-source.mjs` and is imported by this gate and by `conformance:credentials`'s rule 19, the way `assignedValues` is shared by the two spawn gates. It was copied into the credentials gate first and the integrator extracted it; both gates print exactly what they printed before the move, and it skips the parameter list through `closeOf` so a destructured or defaulted parameter cannot be mistaken for the body, which the copy could not do.

**Touching the cache policy?** Add `npm run gate:cache-policy` (about 0.1 s, spawns nothing, opens no profile, launches no Electron) for any commit under `src/main/cache/` or any file that imports from it, being the boot, the diagnostics disk reader and the report text. It proves the policy module imports no file system module and names no durable path in code, that no file reading it calls a deletion API, and that no production file under `src/main` calls a session cache deletion at all, over four fixtures it writes itself so the scanner is seen to fail. The policy is one Chromium switch in the dev shape and nothing anywhere else, because research 69 measured the shipped shape at zero cache bytes over thirty launches; `<userData>/gmux` is durable state and is never a target, and this gate is what keeps a later "cache cleanup" from reaching it in one line. `npm run probe:p166` (about 5 min, 40 Electrons on scratch profiles, spawns no agent) is the full proof and is not in the commit battery.

**Touching a script under `build/` that starts a process it does not wait for?** Add
`npm run gate:background` (about 0.1 s, spawns nothing, opens no socket, launches no Electron) for
any commit under `build/`. It runs inside `npm run build` too, so nothing that builds can skip it.
It is the machine discipline rule above made checkable for everything that is not an Electron. It
asks the question of the FAMILY THAT LEAKED and of nothing else, because asking it of every spawn
under `build/` goes red across most of the directory and stops being a nit, measured on 2026-09-03 at 25 files and 32 call sites by the plain call names alone before any wrapper is counted: a long lived child is an ASYNCHRONOUS
spawn, never a `*Sync` form, that is either `detached: true` or a runner that does not stop by
itself, being a command line carrying `while`, `until`, `for(;;)`, `sleep <n>` or the `sleep` program
itself. The question is asked PER START and not per file: THAT child, held under the name it is held
under, must be killed inside a `finally` block that names it, read by matching braces rather than by
searching for the word, and the kill may be in a helper the same file defines. The call names are
DISCOVERED per file rather than listed, because almost every probe under `build/` declares its own
wrapper and one called `background` would be invisible to a list; one level and not the transitive
closure, which was tried and read 36 of `update-rehearsal.mjs`'s own functions as spawners because
`fail()` reaches a cleanup that reaches a spawn. Nineteen fixtures live in
`build/background-fixtures.mjs`, thirteen of which must make the gate fail, including the exact shape
that leaked, a `finally` that tidies but never kills, and a `finally` whose only kill is in a
comment. **A new shape that walks past it goes in that file in the same commit as the fix.** Run
against the tree before Phase 206 it finds one real leak, being the `/bin/sh` sampler at
`build/probe-remote-env.mjs:350` that was ended on the happy path alone, so a `drive` that threw
left a loop running `ps` with nothing to stop it.

**And this gate's own fix round, for the same reason the known-hosts one has a paragraph.** As first
shipped it asked the question of the FILE: any `finally` anywhere that reached any `kill(` cleared
every start in it, and it read the argument text as it stood. A verifier walked past it five times.
A `finally` belonging to an unrelated inner block, being a helper ending its own short lived
`git status` child properly, cleared a top level sampler ended nowhere. A file that ends ONE loop
correctly and starts a second below it ended nowhere read as green, and that is the likeliest real
regression there is, because a probe that already does the right thing adds a child and leaks it in
silence. The six loop burner with `while :; do :; done` held in a `const`, and `detached: true` held
in a `const OPTS`, were both invisible, which is the value half of the known-hosts lesson this gate
had not taken. And a bare `spawn('/bin/sleep', ['100000'])` was invisible because `sleep <n>` wanted
a space, while the fixture claiming to cover a sleeper spelled it inside a `-c` line. All five are
fixtures now, `assignedValues` is shared by both gates rather than copied, and the twelve original
fixtures read exactly as they did before.

**Touching a script under `build/` that runs ssh?** Add `npm run gate:knownhosts` (about 0.4 s, spawns nothing, starts no ssh and no sshd, opens no socket, reads nothing under the person's home) for any commit under `build/`. It runs inside `npm run build` too, so nothing that builds can skip it. It asserts that no file under `build/` except `build/ssh-run.mjs` hands ssh, scp, sftp or ssh-keyscan to a spawn, that no spawn hands `ssh-keygen` its `-R` or `-F` flag, that no file puts one of the four on a command line a shell is given, that all 19 scripts that run one still reach the helper, and that inside the helper the `-o UserKnownHostsFile=` pair is emitted from exactly one place, refuses an empty value, has no default so forgetting it throws, is prepended so nothing later in an argv can win, and puts Tortie's own record file first. It proves the last four by RUNNING the helper as well as reading it. Run against the tree before Phase 193, it finds 39 unrouted spawns across 18 files and one `ssh -V` on a `/bin/sh -c` line.

**What that gate's fix round is for, because it is the lesson and not the footnote.** As first shipped it printed a claim stronger than what it checked, and a verifier's 21 hostile scripts walked past it 9 times, every one of them resolving under `ssh -G` to the person's own file. The holes were all one pattern: rule 1c existed because `/bin/sh -c "${sshBin} -V 2>&1"` was in the tree, and it was written to that one spelling rather than to the shape, so `-lc`, a command line held in a name, `execSync`, and `shell: true` all sailed through, and rule 1's list of call names was hard coded so a wrapper called `connect` was invisible. That is a fixture written to the bug. Which calls are spawns is now DISCOVERED per file, being the node calls plus any local function reaching one; names that hold a program are read through plain assignments, default parameters, the helper's own `SSH_BIN` export, `||`/`??`/`?:` choices, and constant folding. **All 36 shapes now live in `build/known-hosts-fixtures.mjs`, 32 of which must make the gate fail, and a new shape that walks past it goes in that file in the same commit as the fix.**

**The fix round's own reach was still short, and the committer's round is why the number above is 39 rather than 38.** A name was resolved only from its declaration, so `let file;` followed by `file = sshBin;` on a later line read as nothing, and `probe-control-dialect.mjs:375` handed exactly that name to a raw spawn at the parent commit. The proof it mattered is a whole planted file rather than a fixture: a new probe written in that shape was read by the gate as shipped, which counted it among its files and exited 0 printing that none of them hands ssh to a spawn. So a name is now read from EVERY value it is ever assigned, including another name, three levels deep. Widening a flat table alone would have been wrong in the other direction, because a wrapper's own parameter shares its name with whatever some unrelated line holds: `function sh(file, args)` then reads as a client spawn. So a name is refused resolution inside a function that declares it as a bare parameter, and a parameter carrying a constant default keeps resolving because that default is the file's own text. The count is confirmed by a second scanner written from scratch against the TypeScript AST with a real scope chain, which agrees at 39 across 18 files site for site; where the two first disagreed the bug was the new scanner's, which could not read a program held as an object property. The reach grew by exactly that one real site and the noise did not grow at all.

**Touching the standing contract or the checkers?** Add `npm run conformance:arch` (about 0.3 s, spawns nothing, starts no git, launches no Electron, reads nothing under the person's home) for any commit under `src/main/arch/`, `src/shared/arch.ts` or `src/shared/ipc/arch.ts`. It proves the two claims that decay. The first is that no field of a contract file ever reaches a spawned argv, scanned over every call a whole run composed and proved against a blinded record carrying a hostile element on purpose, because a scan that cannot fail proves nothing. The second is that an invalid row is dropped whole with the file, the field and the reason named. It also pins the format's whole key set in a second file, which is what keeps the research 66 ruling checkable rather than asserted: nothing in `docs/arch/` may ever name anything Tortie runs. The gate spawns no git at all, because the checkers take an injected git seam and the committed fixture carries git's own wire bytes, so it asserts over every argv a run composed rather than over the ones a live repository happened to need.

**Touching the level 2 module view?** Add `npm run conformance:arch:modules` (about 0.3 s, spawns nothing, starts no git, launches no Electron, reads nothing under the person's home) for any commit under `src/main/arch/modules.ts`, `src/shared/ipc/arch-modules.ts`, `src/renderer/arch/ArchModules.tsx` or `src/renderer/arch/arch-modules.css`. A cap nobody has crossed is a comment, and the fixture the `conformance:arch` gate uses holds twelve tracked files, which cannot reach either of this view's two. So this one drives both boundaries exactly: 30 files draw boxes and 31 do not, 200 participating files draw a matrix and 201 do not, and it fails if either fallback never fires at all. It also pins the keys of a drawn box in full, because "no count badge on any node" is a refusal a later round can undo in one line, and it re-derives every part twice and over reversed inputs so a map iteration order shows up as bytes that moved. Phase 64 shipped it classified in `build/verification-checks.mjs` and named in no document, which is how a gate decays: `npm run build` passed and nobody was told to run it.

**Touching the hue or the ramp's own lightness?** Add `npm run conformance:hue` (about thirteen minutes since Phase 218 took the ablations to 45, measured at 747 s at the parent and 759 s at HEAD; it was eleven minutes and 654 s when Phase 214's fix round wrote that number, against ten minutes and 570 s when Phase 213's fix round wrote that number; it was thirty three seconds when Phase 210 wrote that number and the walk has tripled since, so budget for it rather than being surprised by it, one node running the pinned tsx, launches no Electron, starts no tmux server, spawns nothing else, makes no request, reads nothing under the person's home) for any commit under `src/shared/chrome-hue.ts`, `src/shared/chrome-ramp.ts`, `src/renderer/theme/hue.ts`, `derive.ts`, `presets.ts`, `floors.ts`, `frame-stops.ts` or `apply.ts`, `src/renderer/settings/AppearanceSection.tsx`, which rule 31 SCANS, `src/renderer/terminal/theme.ts`'s text rule, `src/renderer/editor/monaco-theme.ts`, `src/main/settings/chrome.ts` or the neutral values in `src/renderer/styles/tokens.css`. It runs the SHIPPING rotation and text rule under node over the shipped tokens and re-derives contrast with culori's full entry. It walks every whole degree of the circle at Normal and every fifth at Raised and High, the wrap, a fractional hue, and the synthetic ground, the whole ramp lifted from graphite to white in 171 steps, and pins fourteen rules: the default derives nothing; every hue but 222 writes exactly the eight neutrals, being the five fills and the three hairlines, because `--border-strong` is on the ramp too; every neutral keeps its shipped OKLCH lightness within 0.005 (measured 0.002), which is the whole reason the space is OKLCH and not HSL, where the same rotation drifts 0.034; the turned hue is the shipped hue plus an OFFSET from each token's own OKLCH hue, because 222 is the HSL reading of a ramp that sits at 264 to 274 in OKLCH and an absolute hue would not be byte identical at the default; the ramp keeps its order; every pinned ratio holds at every hue and level for the backgrounds AND the four text tokens against every ground each sits on, the hairlines within a band of theirs; the text flips at ONE threshold, the canvas luminance where black and white text tie, sqrt(0.05 x 1.05) - 0.05, crossing once, with the shipped text kept below it and lifted only when a ground puts it under its floor, and every text colour solved dark to its shipped ratio above it; and no hue reaches the flip by rotation, since the canvas chroma is 0.006 and the rotation tops out at canvas Y 0.0073, which is why the walk to white is synthetic and reached by no setting. **PHASE 210 ADDED SEVEN RULES TO THE SAME GATE AND EIGHT ABLATIONS BESIDE THEM**, because the person can now MOVE the ramp as well as turn it, two stops each of a shade and a depth. The walk gains a third and fourth axis and is exhaustive on both, all 49 shade and depth pairs against EVERY WHOLE DEGREE at all three contrast levels, 61,446 derivations, plus the four highlight schemes at every fifteenth degree; the whole degree step is not caution, because the failures sit in CLUSTERS a few degrees wide that a fifteen degree step walks over. Rule 15 is the OFFERED REGION, pinned as a table in this file AND as `FRAME_REGION` in `presets.ts`, which is what the two sliders offer from, both asserted equal to what the walk measures and both contiguous on each axis so a slider cannot skip a stop; the region is the worst case over every hue, contrast and scheme because the five appearance settings compose in any order. Rule 16 is every floor at every offered frame, including the RENDERED STEP, being at least two eight bit levels between adjacent rungs. TWO IS THE SMALLEST STEP ABOVE THE ROUNDING FLOOR, one level being the least difference eight bits can express, so the floor asserts that a rung is still a rung rather than that the ramp keeps its shipped spacing, which is a stricter rule that would refuse frames a person can read perfectly well. THIS SENTENCE ORIGINALLY SAID TWO WAS WHAT THE SHIPPED RAMP HOLDS AT ITS TIGHTEST and the Phase 210 verifier refuted it: the shipped ramp holds FIVE over this rule's own six pairs at every whole degree and all three contrast levels, at `--bg-sidebar` on `--bg-canvas` at hue 6, and one level is where the shipped rotation already puts `--border` on `--bg-active` at hue 44, the pair DESIGN.md section 1.10 records as an edge that does not exist at 1.013:1. The floor did not move and the region did not move by one cell; the reason did, in presets.ts, DESIGN.md section 1.1, the gate's rule 16 and here. Rule 17 asks the ORDER question at every cell including the refused ones, in OKLCH lightness where the arithmetic runs, because the transform is affine with a positive slope and what a refused stop loses is eight bit distance and never order. Rule 18 is that the SHIPPING floor predicate, which is what the sliders stop on, agrees with the walk at every point, and it went red 6,937 times while the two read a slack of zero differently. Rule 19 is that the frame moves the ground under the accent, the git decorations and the graph lanes and never them. **RULE 20 CORRECTS THE PHASE'S OWN CHARTER**: the entry said the text flip would become reachable and it does not, because the git decorations on `--bg-active` stop the ramp at canvas Y 0.0147, the lightest canvas any offered frame reaches over every whole degree, being `#1d2123` at hue 186 shade 2, against a flip at Y 0.1791 that is twelve times away; at the shipped hue that lightest canvas reads Y 0.0138, which is the number this paragraph quoted before the verifier pointed out it was one hue's reading rather than the walk's maximum, so the rule asserts the opposite and rules 9 to 11 keep the flip proved on the synthetic ground. Rule 21 is that the shipped pair still writes nothing and every other stop moves. **RULE 8 IS NARROWED to the shipped shade and depth** in the same change, because its three pinned hairline ratios are exactly what the depth control moves, 1.130 at the narrowest stop against 1.673 at the widest. The ablated copies walk every forty fifth degree PLUS THE WITNESSES, being the hue at which each of the fourteen refused cells actually fails, without which a coarse walk turns the region rule red for a reason that is not the ablation. Twenty ablations go red one clause at a time, the first of them the rotation done in plain HSL. **PHASE 214'S RULE 31 IS ABOUT THE WHOLE FRAME GROUP AND NOT ONE CONTROL**, and the committer's round is why that sentence is here: a control that cannot move is not shown, so paper carries no Shade row, and the rule first asked that of the ROWS alone. Reset is drawn on every base, it wrote all three frame fields whatever base it was pressed on, and a person on paper who nudged Depth and pressed it lost the shade they had chosen on dark. The reset patch is now composed by the shipping `resetFrame` from the same two booleans the rows are drawn from, run by the gate at every frame each base offers, with the button's own wiring scanned the way the rows' guards are, and two ablations that each go red naming the write. **`npm run probe:p210`** (about two minutes after the build, TWO Electrons one after the other and never at once, on one scratch profile and the gmux-p210 socket, spawns no agent, spends no token, opens no keychain) is Phase 210's app run and is not in the commit battery: it drives six frames and reads the same six surfaces off the DOM at each, proves the round trip to the shipped pair is byte identical with an EMPTY override map, drives the flip through the Phase 207 harness ground since no stop reaches it, and then reads the SETTINGS window's own resting face to prove NO DEGREE IS DRAWN on it, that the eight starting colours are named, that the frame came back from the settings file, and that a slider asked for a stop the region refuses stops at the edge and says why. `npm run probe:p207` (about two minutes after the build, TWO Electrons one after the other and never at once, on one scratch profile and the gmux-p207 socket, spawns no agent, spends no token, opens no keychain) is the app run and is not in the commit battery: it reads the titlebar, the sidebar, the Pierre tree root, the body, the terminal host with its live xterm theme and the Monaco editor off the DOM at the default, at a dark hue, at a flipped ground and at the threshold from either side, proves the six agree on both ground and text at every one and that the terminal and editor foregrounds are one byte, returns to the shipped bytes, then relaunches to prove the hue comes back from the settings file at boot and that scheme, contrast and hue compose to one override map in all six orders. **The canvas answer is that main carries the hue.** `src/main/settings/chrome.ts` composes the window fill from the persisted hue through the same shared rotation at both window constructions and keeps a live window in step, so `--bg-canvas` IS on the ramp; the first frames before the renderer's settings read still paint the shipped `#131417`, at most 0.014 in OKLab from the turned one, which is the Phase 62 limit for every override and is unchanged. The diff view keeps its shipped ground because `@pierre/diffs` takes a theme by name once at registration; that is the stated limit.

**PHASE 214'S FIX ROUND WIDENED RULE 30 FROM ONE METRIC TO EIGHT, and the reason is the lesson rather than the count.** The lane floor was asked in the metric research 24 used and the palette was solved in, being the minimum over Vienot protanopia and deuteranopia. That metric says nothing about tritanopia at all, so it clears a paper palette in which two lanes collapse for a tritanope, and the ablation plants exactly that: a `--graph-lane-5` of `#000088` reads 38.1 in the solved metric and 28.1 against the teal of lane 3, which is what turns the rule red. The floor is now asked of eight model and deficiency combinations over three published models, being Vienot 1999 for protanopia and deuteranopia, and Machado 2009 and Brettel 1997 for all three, and paper's worst is 36.1 at lanes 3 and 5 under deuteranopia. **EIGHT AND NOT NINE.** The phase's measure step ran a ninth, being the single plane extended to tritanopia in the same LMS basis, and published a worst pair "over six published dichromat models" that included it. That arm is degenerate: its two matrices round trip to the identity to 4.1e-9 so it is not a transcription error, but on the tritan plane the reconstruction gives red and green the SAME coefficients, 0.034733 on L and -0.036998 on M, so R equals G in 140,460 of 140,608 colours and it is a red green confusion wearing the name of a blue yellow one. It leaves a blue only difference of 128.0 completely untouched where Machado reads 49.3 and Brettel 34.7. **It held a fixed white point and it was idempotent throughout**, which is exactly what the measure step checked, so `proveLaneSimulation` now asks every arm to move something, asks each tritanopia arm to confuse blue with yellow AND to spare red against green, and asks Brettel's six half planes to be singular and to agree on their own shared boundary, which is the check a mis-scaled anchor cannot pass. `build/p214/cvd.mjs` carries the same models with the Brettel matrices re-derived from the published construction to 0.011 a coefficient, and `build/p214/out-cvd-prove.txt` is what that printed.

**PHASE 213 ADDED SIX RULES TO THE SAME GATE AND THIRTEEN ABLATIONS BESIDE THEM**, because there are now TWO base palettes and a light one can be half done in any of the six places that do not read a token at draw time. Add the gate for any commit under `src/renderer/styles/tokens.css`'s light block, `src/renderer/pierre/theme-bridge.ts`, `src/renderer/editor/monaco-theme.ts`, `src/shared/window-chrome.ts` or `src/renderer/theme/frame-stops.ts` as well. Rule 22 walks EVERY rule the dark base holds over the light base too, by the same probe over the light block of tokens.css, and it is where rule 20 INVERTS: the text family reads dark at all 61,446 points on paper, so the flip Phase 210 could only reach on a synthetic ground is where this base lives. Rule 23 is the light offered region, which is FOUR of the 49 pairs against dark's 35, because the light palette was solved AT its floors so the accent could be text at 4.5:1 and the status dots could clear 3:1 on the active row, which is the note Phase 210 left open on dark and is closed here. Rule 24 is the six non token surfaces carrying a second theme, being the terminal ground `#131417` to `#f5f7fa` with all 21 slots moved and xterm's `minimumContrastRatio` 1 to 4.5, Monaco `vs-dark` to `vs`, Pierre's `gmux-dark` and `gmux-light` both named in the theme pair so the diff follows the root, the tree host no longer pinning a colorScheme of its own, the window fill main composes, and the shadow hosts given a color-scheme in globals.css. **RULE 25 IS THE ONE THAT MATTERS MOST: DARK IS BYTE IDENTICAL** to the parent commit 02fd5ed by four digests, being the tokens, the terminal theme, the Monaco theme and the Pierre theme, plus both window fills and the xterm floor. Rule 26 scans every stylesheet and theme object for a colour literal outside the six theme constant places and the four named exemptions, over 1,236 files, proved on 8 fixtures of which 5 must make the scan fail. **RULE 27 IS THE FRAME CARRIED ACROSS A SCHEME CHANGE**, and it is a defect the phase found rather than a property it assumed: the Scheme control writes ONE field, so 31 of the 35 frames a person may be holding on dark are ones paper cannot draw, 20 of them putting `--accent-text` under 4.5:1 and 8 inverting the ramp ORDER, reachable by setting a shade on dark and then choosing Light. `frameForBase` brings a carried frame to the nearest stop the base in effect offers and PERSISTS NOTHING, so choosing Dark gives back exactly the frame that was chosen; the rule walks both regions through the shipping function and reads 35, 4, 31 moved and 0 left outside, and its ablation must still LOAD and answer, because a copy that fails to load turns the rule red for the wrong reason and proves nothing about the arithmetic. `npm run probe:p213` (about four minutes of driving after the build, FIVE Electrons one after the other and never at once, on one scratch profile and the gmux-p213 socket, spawns no agent, spends no token, opens no keychain) is the app run and is not in the commit battery: it boots dark and reads the first frame as one colour, opens a session, a Monaco file, a Pierre diff, a Redline and the Architecture map, reads every surface on dark, switches with the screencast running and grades the crossfade frame by frame for a half palette, reads every surface on light, compares the window to research 80's mock BY RECTANGLE AND BY COLOUR allowing one level per channel, proves reduced motion switches in one frame and that the dark reading comes back byte for byte, reads the window fill through main's own inspector, flips the Mac ten times in a second under Match the Mac, then boots twice more to prove light boots light from the first frame and that a hand edited scheme reads as Dark. **THE FIX ROUND ADDED RULE 28 AND A FOURTH LAUNCH.** Rule 28 is the graph lanes on BOTH bases, because lane colour is identity and the test that held that property was narrowed to the first `:root` block in this phase, so the light lanes were measured by nothing: they carry TWO pairs a dichromat confuses against the dark base's one, at 26.9 and 12.4 where dark's is 21.2, and the rotation's soft-avoidance map in `src/renderer/scm/graph/colors.ts` named the dark pair alone. The rule re-derives every base's weak pairs from tokens.css with the four var() aliases resolved, with its own Vienot simulation, and fails unless every one of them is in the shipping map both ways round; it also asks WCAG 1.4.11's 3:1 of every lane on its own base's three row grounds, reading 4.41 on dark and 3.52 on paper. Add the gate for any commit under that file too. 12.4 is the stated limit rather than a defect fixed: it needs a different light `--git-conflict` and `--git-added`, which is a palette change. Launch D of the probe is the ordinary in-session switch read on the FACE, which is what nothing read before: 12 findings at the parent and 0 after. `P213_ONLY=D` drives that launch alone in about a minute and a half and changes nothing any launch asserts. **RULE 29 IS THE OTHER HALF OF THE SAME ROUND**, and it is a floor nothing applied. xterm applies `minimumContrastRatio` at DRAW time and changes no cell, so the phase vendored xterm's own lift into `src/renderer/terminal/capture/contrast.ts` for the BUFFER path and then wired it to nothing: Copy as HTML, a history capture and a selection scrolled out of view all carried the colour the agent asked for while the screen carried the colour the person could read, which on paper is Claude Code's `#ffffff` at 1.07:1, a page of invisible text. `serializeAsHtml` now takes a `contrastFloor` that defaults to the live pane's own, so dark, whose floor is 1, is byte identical, and it measures a cell against the ground the markup really puts it on, being the terminal's on a picture and the document's white on the clipboard flavour. Rule 29 pins the five cells research 80 section 1.3 measured on both grounds, the halved dim floor, the glyph exemptions and the fact that the serializer calls it at all, with a thirty fifth ablation that unwires the call and goes red. The pins are the ARITHMETIC's answers and not the research table's pixels, `#ffd700` going to `#867000` here where the photograph read `#837122`, because a glyph pixel is the drawn colour blended with its ground and xterm's walk only ever subtracts, so a colour whose blue channel is 0 cannot gain one. Add the gate for any commit under `src/renderer/terminal/capture/contrast.ts` or `serialize.ts` too. `--self-test` proves its graders on 36 fixtures and launches nothing.

**THE COMMITTER'S ROUND CLOSED THE LAST ONE, AND IT IS A SLIDER RATHER THAN A PALETTE.** Rule 27 above promises that a carried frame is brought to the stop the new base can draw and that NOTHING IS PERSISTED, so choosing Dark gives back exactly the frame that was chosen. The two Frame sliders were the hole in that promise, because a slider drew the BROUGHT stop and persisted whatever a move clamped to. On paper the whole region is the single shade row at 0, so the Shade slider is inert, and one arrow key or one stray click on it wrote the shipped shade over the shade the person chose on dark. Driven at the parent build with real key and mouse events: shade -2 and depth 3 set on dark, and after one ArrowLeft on paper the file read 0, with the depth destroyed by the refused move too and Dark coming back at 0. The guard is one comparison in `stopSliderPick`, being that a move whose landing place is the stop already in front of the person writes nothing; it is a comparison against the DRAWN stop and not the inverse of the refusal, because the DESIGNED refusal still has to move, a drag from 1 to 3 where the edge is 2 landing on 2 and saying why. On the dark base the drawn stop is always the persisted one, so the only writes it removes are writes of the value already there and nothing a person can see changes. Launch E of the probe drives all four arms with real Input domain events and reads the settings FILE rather than the face, because the face is a clamp and the file is the promise: 7 findings at the parent and 0 after, with the control arm proving that an arrow INSIDE the Depth range still writes. `src/renderer/settings/__tests__/p213-carried-frame.test.ts` pins the same guard over both shipping region tables and goes red on three of its four cases when the comparison is ablated to a bare true, so `npm test` is this one's gate rather than the hue walk, which never touches a control.

**PHASE 218 ADDED RULE 32, AND THE THING IT GUARDS WAS IN NO FLOOR FAMILY AT ALL.** `--status-idle` and `--status-exited` were the same `#6e7583`, the solid IDLE dot and the hollow EXITED ring, both drawn on `--bg-active`, and they were exactly the shape the floors exist for: a token that never moves over a ground that does. Shipped they read **3.149:1**, 0.149 over the 3:1 WCAG 1.4.11 asks of a non text mark; over the 35 frames the Appearance controls offer they fell to **2.591 at Normal** (shade 2, depth 0, hue 134), **2.402 at Raised** and **2.193 at High** (shade -2, depth 3, hue 63), with **7 of the 35 under the floor at Normal alone and 19 at some contrast level**, one of them a single stop from the default. It was not a Phase 210 defect: at shade 0 depth 0, all Phase 207's circle offers, the pair already reached 2.701 at High. The record could not settle whether that count was 7 of 35 or 11 of 40, and it is **7 of 35**: the grid is 49 pairs, the region is 35, 40 is the size of nothing this tree offers, and the count is 7 at every hue step from 1 to 180 degrees and under all four highlight schemes. **AND IT COULD NOT SIMPLY BE PINNED.** With the shipped hex and a 3:1 pin added, the region collapses from 35 cells to 16 and the SHIPPED DEFAULT falls outside it, which is a bug wearing a floor's clothes. So the phase moved the colour and put the floor in behind it: `#8b93a1`, the shipped OKLCH chroma at hue 262 lifted from L 0.561 to 0.661, worst **3.281** over every offered frame at every contrast level, 4.712 on the shipped fill, and the region does not move by one cell. **The price is arithmetic and is written into DESIGN.md section 1.3 rather than hidden:** a fixed colour needs relative luminance at or above **0.26021** to clear 3:1 on `#424238`, the lightest active fill any offered frame reaches, and `--text-muted` sits at 0.24919, so the idle dot is now marginally LOUDER than the metadata beside it where it used to be quieter. There is no darker answer, and a search for one is wasted time. **RULE 32 IS TWO HALVES ASKED SEPARATELY**, because a later round can undo either without touching the other. The arithmetic half walks the cells the PINNED REGION TABLE names rather than the cells the walk found feasible, at every contrast level on both bases, and reads a number the probe took with a list of ITS OWN held beside the walk: once the floor is in the family, a colour that broke it would drop its own cells out of the feasible set and take rule 16's reading with them, and a rule reading only `worstChroma` would go blind the moment somebody took the pin back out. The structural half runs the shipping `chromaticPinsFor` and asks what it really names on each base, which is the half that catches a round that removes the pin and leaves the colours clearing the floor by luck. **Two ablations, one per half, and both go red**: the greys put back where they were, which plants the hex that really shipped rather than an invented one, and the family taken back out of `chromaticPinsFor('dark')`, which leaves every ratio green so only the structural half can see it. **Rule 25 kept its full strength rather than taking a new pinned digest**: the probe now emits the dark `:root` block with the two greys put back to `#6e7583`, and THAT digest is asked against 02fd5ed's and comes back byte identical, which proves only those two declarations moved in the whole block; the live digest is asked to DIFFER, or the greys never moved. Add the gate for any commit under `src/renderer/styles/tokens.css`'s status values as well as its neutrals. `--status-failed` is deliberately NOT pinned and reads **3.013** at the same binding frame; pinning it costs the region nothing today and would make a 0.013 margin the binding number of the whole region, so DESIGN.md section 1.3 records it in prose and the next palette change must not take it under. **`npm run probe:p218`** (about two minutes after the build, ONE Electron on a scratch profile and the gmux-p218 socket, with HOME inside the scratch directory, two plain shell sessions on that socket, no agent, no token, no keychain, no request) is the app run and is not in the commit battery: it reads the disc and the ring off REAL selected rows at five frames, being the shipped one on graphite, the worst graphite offers, that same frame with the parent's two declarations planted on the live root, and the shipped and worst frames on paper, and it reads 4.712, 3.281, 2.193, 3.412 and 3.007, every one of them the number the gate publishes, with WCAG arithmetic of its own rather than culori's. It finds the ground by walking up from the dot to the first opaque fill instead of trusting a token name, and it reads the row's aria-label beside the mark, because a tab and a list row are too dense for a visible label. TWO LIMITS ARE STATED IN THE FILE RATHER THAN HIDDEN: the parent reading is a plant of the two declarations and not a rebuild at 4e1b98c, which rule 25's digest is what makes honest; and the ENDED ring is read by putting the shipping `dot-ended` class on the real dot in the real selected row, because `remain-on-exit failed` destroys the pane of a session that ends with status 0 and Tortie draws what is left as SAVED with the solid disc, so a clean `exited` row belongs to an unreachable or removed session and needs a remote machine the run refuses to touch. `--self-test` proves its graders on 23 fixtures and launches nothing. **THE FIX ROUND REFUTED THIS PHASE'S OWN MINIMALITY SENTENCE, AND THE COLOUR DID NOT MOVE.** tokens.css section 1.3, `STATUS_PINS_DARK` and DESIGN.md section 1.3 each called `#8b93a1` the LEAST lift that clears the floor, and it is not: the least at that chroma and hue is L 0.639, `#858c9a`, which reads **3.005** on `#424238` and keeps the SAME 35 cells, re-derived through the shipping predicate over the whole walk, so a smaller answer works and the minimality was never measured. What WAS measured is why the phase went past it, being that its 0.005 of margin is a ninth of the **0.047** that one eight bit level of that fill is worth, so a ground one level lighter takes it under, and that it sits **dE2000 1.13** from `--text-muted` `#838996`, about one just noticeable difference, so the dot and the metadata beside it would read as the same grey; `#8b93a1` carries 0.281, about six levels of that ground, at dE2000 3.44. WHERE IT STOPS BETWEEN THOSE TWO IS A JUDGEMENT AND NOT A SOLVED MINIMUM, since `#88909e` already clears the floor by 0.158 and the muted text by 2.48, and the three files say that now instead of claiming an arithmetic they do not have. `src/renderer/theme/__tests__/p218-least-lift.test.ts` pins those numbers against the SHIPPING tokens under `npm test`, in about 140 ms, and it goes red on five of its six cases with the greys put back and on two with the muted text moved one step, so the corrected sentence cannot decay the way the first one did; `build/p218/least-lift.mts` prints the whole ladder over the real walk. **AND THE 3.013 IS RIGHT.** The fix round was handed a reading of 3.073 for `--status-failed` at the binding frame and re-measured it: 3.073 is the RESTING `#e5655e`, while High contrast PAINTS `#f45450`, which is what the walk, the floors and the row all read, and the contrast level moves all three chromatic status colours and neither grey. Six offered frames tie at the binding fill `#424238`, being shade -2 depth 3 and shade 2 depth 0 at hues 63, 64 and 65, so naming either cell names the same reading; `build/p218/derived-at-frame.mts` prints all three.

**Touching the shared IPC contract, the manifest schema, a `gmux.*` storage key, a `GMUX_*` env name or a harness smoke mode?** `npm run gate:contract` (about 0.8 s, spawns one plain node over a scratch directory, opens no live manifest, launches no Electron) byte compares the inventory `build/contract-inventory.mjs` emits against `docs/audits/contract-baseline.txt`. It runs as the last step of `npm run build`, so nothing that builds can skip it. A deliberate change regenerates the baseline in the same commit with `node build/contract-inventory.mjs --out docs/audits/contract-baseline.txt` and says which lines moved and why in the commit body. A silent diff fails the build. Phase 171 put it there because the baseline had sat 16 invoke channels, 22 env names and one smoke mode behind the tree for thirteen phases while the check sat in no gate, which the audit of 2026-08-30 named plainly: a deterministic alarm nobody runs is documentation.

**Changed how sessions attach, split or close, how a surface mounts or unmounts, or anything Phase 167 measured?** Run `npm run probe:p167` (about 10 to 20 minutes since Phase 200 put the surface profile under a CPU throttle, one Electron on a scratch profile and a scratch tmux socket, spawns no agent, spends no token; not in the commit battery). It is the Phase 167 scale scenario as a check: one app run drives project switches, the open and close of Catch Me Up, Architecture, a Monaco file, a diff and a markdown preview, and the split, close and reattach of four real shell sessions a cycle, in blocks, and grades the blocks by the plateau rule Phase 167 adopted. Renderer heap after collection must stop growing block over block, DOM nodes and event listeners must stop growing on the surface profiles, and main must hold exactly the `/dev/ptmx` and `/dev/ttys` descriptors it started with, which is Phase 167 finding 1. Main's footprint is printed and not judged, because its climb through the split profile is V8's young generation reaching its cap and Phase 167 owned it. EVERY block to block pair is judged and the worst one is the verdict, in all three dimensions, being heap, nodes and listeners, rather than the last pair only, because Phase 200 measured a run that retained 1,662 nodes a block and then released them during the last block, whose last pair read negative, and the old rule printed "every driven profile plateaued" over it. The surface profile also drives at a quarter CPU speed and reads one thing that is not a race: with the diff up, under the reduced motion this drive emulates, the container's computed `transition-property` must be `none` and no CSS transition may be running. That reading is the deterministic half of this check, and the node count is the sensitive half: the throttle WIDENS the window the retention race falls in and does not close it, so the node count catches a transition still running when its element is removed only when the machine is busy, and reads flat on an idle one at any throttle, while the motion reading is red at the parent on any machine. **PHASE 220 GAVE THE SPLIT PROFILE THREE RULES AND NAMED NO OWNER**, and the second half of that sentence is the point: the profile failed two runs of three at `b5cc017` at about 5 MB and exactly 1,020 DOM nodes a block, seven runs afterwards over a renderer BYTE IDENTICAL to the one those failures measured came back clean, and neither Phase 200 lever, being the CPU throttle and a second app doing the same work beside it, reproduced the slope. So Lifecycle's split half is neither repaired nor explained and stays at 2, and what changed is that the ruler can name an owner the day it returns. The DETACHED CENSUS asks the page through `Runtime.queryObjects`, a different mechanism from the Performance counters everything else here reads, for every element it still holds, counts the ones no document can reach grouped by the root of each detached tree, and asserts it under a budget of 50; a detached tree is never drawn and is never Past Sessions, so it can be asserted where `Nodes` could not. The WORKLOAD FLOOR fails a run as INCONCLUSIVE when a block discards under three quarters of `cycles x 4` sessions, read from the app through `sessions.listRemoved` rather than from the DOM, **which refuted the measure step's own inference**: profile d opens no Past Sessions modal at all, so a flat node count is this profile's healthy reading and not an absent workload, and every flat run reads 0, 24, 48 and 72. The PLANTED LEAK is on by default, being 24 detached trees of 43 elements, and the census saw exactly 1032 of them, the grader raised 1 finding and 0 were left after release. `build/heap-retainers.mjs` is the reader beside it, walking a `.heapsnapshot` breadth first so every node gets its SHORTEST retaining path, never letting a `weak` edge be a retainer, and reading V8's `detachedness`; `--self-test` proves it on 5 fixtures. `node build/probe-p167-scale.mjs --self-test` proves the grader on eighteen fixtures and launches nothing. Knobs: `P167_BLOCKS`, `P167_CYCLES`, `P167_PROFILES`, `P167_SURFACES` (which of the five the surface profile drives), `P167_CPU` (the throttle, default 4, 1 turns it off and the run says so), `P167_HEAP_MB`, `P167_NODES`, `P167_LISTENERS`, `P167_DETACHED`, `P167_SNAPSHOT`, `P167_PLANT`, `P167_CPU_PROFILES`, `P167_CENSUS_ROOTS`.

**Touching the control client's greeting deadline, the control plane or the machine context?** Run `npm run probe:controldeadline` (about 37 s, no Electron, one scratch sshd and one scratch far-side tmux server under `<tmpdir>/p83-deadline-<pid>`, the sockets `gmux` and `default` refused by name, the operator's own server counted before and after; not in the commit battery) for any commit under `src/main/tmux/control-client.ts`, `src/main/machines/context.ts` or `src/main/machines/control-plane.ts`. **PHASE 220 DID NOT REPAIR THIS PROBE, IT GAVE IT ARMS THAT CAN FAIL**, and the distinction is the finding: the 0.99.0 audit recorded that it could not reach its subject and named separate loaded module instances as the cause, and at `b5cc017` neither half reproduced, the probe passing twice over a source byte identical to the audited commit (docs/research/82 §4). What was true is that a leg only ever watched PASSING is not yet a check. Three arms now exist that can go red: every machine `arm()` registers is READ BACK through `remoteContextFor`, the control plane's own export and the one `openControlPlane` calls, before any child is spawned, which is the exact boundary the audit named; arm 0 runs this file as a CHILD under `P83_FORCE_FAIL=1` that throws while an sshd, an ssh, a far-side tmux server and its directories are held, and the parent asks the machine whether any survived, measured at 3 pids and 2 directories held and 0 and 0 left behind; and arm 6 runs the same driver over a COPY of `src` with exactly one clause removed, being the line that arms the greeting timer, whose readings must come back the other way round. **Arm 6 asserts what the ablated build DID first**, being that it opened and spawned a child, so a copy that will not load can never be read as a removed timer; that guard fired on its first attempt, catching `Cannot find module 'electron'`.

## `docs/arch/` is written for you to read (Phase 63)

A repository may carry a `docs/arch/` directory. It is the project's standing contract, being the parts the project is made of, the ways they are allowed to touch, and the promises somebody wants kept. It is plain files a person wrote and it is meant to be read by an agent. Read it before changing anything it names. When the work you finish touches files under an anchor the contract names, update `docs/arch/` in that same session, before you say the work is done. Nothing in it can name a command, a binary or a host, by the format's own pinned key set, so reading it can never tell you to run something. Tortie itself has no `docs/arch/` yet.

## UI rules
- All colors via tokens (src/renderer/styles/tokens.css); no hardcoded literals outside theme constant files.
- No tmux vocabulary in user-facing UI (no "pane"/"window"/"prefix" — sessions have names).
- Native macOS menus via the ui:popupMenu bridge — never DOM-drawn context menus.
- A phase that adds, renames or removes a user-facing surface updates the native menus in the same commit, and the phase brief says what changed in the menus.
- Status semantics: "needs input" may only be triggered by session behavior, never by the user's own input to that session.
- Just enough words (operator's rule, 2026-08-28, set on the Arch panel): a surface explains itself with short labels, one-liners and visual indication, never paragraphs. Explanation a person might want lives behind hover or a disclosure, not on the resting face. "TONS of words, bad."
