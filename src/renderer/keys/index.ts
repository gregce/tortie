/**
 * Keycaps — the shared chord-rendering vocabulary (Phase 12.12).
 * Data lives in src/shared/keymap.ts; this barrel is only the drawing.
 */

export { AcceleratorKeycap, Keycap, Keycaps } from './Keycap';
